# AICOUTE — Guide Claude Code

## Contexte
SaaS de compagnon conversationnel IA pour personnes âgées/isolées.
- **Aidant** (caregiver) : dashboard web, crée les bénéficiaires, configure les sessions, lit les rapports
- **Bénéficiaire** : reçoit des appels vocaux IA sur l'app mobile Expo

## Stack technique
| Composant | Technologie |
|-----------|------------|
| Dashboard web | React 18 + Vite + TypeScript + TailwindCSS |
| App mobile | Expo (React Native) |
| Base de données | Supabase (PostgreSQL + RLS + Auth) |
| Edge Functions | Supabase (Deno) |
| Voix temps réel | **Multi-moteur** OpenAI Realtime GA (`gpt-realtime-2`) ou Google Gemini Live (`gemini-3.1-flash-live-preview`). **Décision produit (pour l'instant) : Gemini-only côté UI** — le sélecteur de moteur a été retiré de la config IA ET du wizard, `beneficiaries.preferred_engine` est forcé à `gemini` à l'enregistrement. La plomberie multi-moteur subsiste (2 bridges, colonne `preferred_engine`, tarifs dispatched) et OpenAI reste **testable par téléphone via `/admin/sante`** (section « Test d'appel OpenAI / Gemini »). Voix par bénéficiaire pour les appels planifiés : `gemini_voice` (Gemini) ; `ai_voice` (OpenAI) conservée en base mais plus exposée. Catalogue de voix + échantillons audio écoutables dans l'onglet Configuration IA : `packages/shared/src/voices.ts`. Démo vitrine : **Gemini en dur** (toggle moteur retiré ; reste le choix de la langue). Voix bénéficiaire (app mobile Expo) : phase 2, non branchée pour l'instant. |
| Emails | Resend |
| Déploiement web | Netlify |

## Structure monorepo
```
modect/
├── apps/
│   ├── web/          # App web UNIQUE (React + Vite) : vitrine (src/marketing) + back-office (src/pages)
│   │                 #   → www.aicoute.fr (vitrine) + app.aicoute.fr (back-office), routage par sous-domaine
│   └── mobile/       # App bénéficiaire (Expo) — couche vocale à migrer (phase 2)
├── services/
│   └── voice-bridge/ # Service Node (Render) — pont multi-moteur démos vitrine + appels AICOUTE.
│       └── src/engines/  # openai-bridge.js, gemini-bridge.js (démo téléphone),
│                         # gemini-bridge-web.js (démo navigateur), audio.js (µ-law ↔ PCM),
│                         # modect-call-bridge.js (appels AICOUTE OpenAI),
│                         # modect-gemini-bridge.js (appels AICOUTE Gemini)
├── supabase/
│   ├── migrations/   # migrations SQL
│   └── functions/    # Edge Functions Deno
├── packages/
│   └── shared/       # Types + cœur Realtime partagés :
│                     #   realtime.ts (RealtimeSession, OpenAI WebRTC)
│                     #   geminiLive.ts (GeminiLiveSession, WS proxy via voice-bridge)
└── test/             # Référence : test fonctionnel WebRTC GA (à supprimer après validation)
```

Note : `apps/web/public/gemini-audio-worklet.js` (servi statique) — AudioWorkletProcessor qui downsample le micro 48 kHz → PCM16 16 kHz pour le mode web Gemini.

## Base de données (tables principales)
- `profiles` — extension de auth.users (trigger `handle_new_user`). **Identité aidant (2026-06-14)** : `account_type` (`individual` personne physique | `organization` personne morale, CHECK, défaut `individual`) ; `first_name`/`last_name` (identité physique OU contact principal d'une morale) ; `company_name` (raison sociale, morale uniquement) ; adresse postale `address_line`/`postal_code`/`city`/`country` (défaut `France`). `full_name` est **conservé et synchronisé** (= « prénom nom » physique, = raison sociale morale) pour ne pas toucher aux listes/en-têtes/emails qui le lisent — calculé par `computeFullName` (`packages/shared/src/types.ts`, **dupliqué** côté edge dans `admin-update-caregiver` car Deno n'importe pas le package, et par le trigger SQL `handle_new_user` au signup, migration `20260614000001`). Saisis à l'inscription ([Register.tsx](apps/web/src/pages/auth/Register.tsx) : type + identité, conditionnel) et éditables dans `/compte` → Mon profil + `/admin/comptes/:id`. Sélecteur de type partagé : [AccountTypeToggle.tsx](apps/web/src/components/AccountTypeToggle.tsx). Champs optionnels (rien ne bloque pour l'instant).
- `beneficiaries` — profil bénéficiaire + config IA. **Identité (2026-06-09)** : `birth_date` (DATE, date de naissance complète — pour l'âge précis + anniversaires ; `birth_year` INT **conservé et synchronisé** à partir de `birth_date` par l'UI, pour ne pas toucher au calcul d'âge des Edge Functions / du prompt) ; `gender` (UI = Femme/Homme, « Autre » retiré côté UI, colonne `CHECK male|female|other` inchangée) ; téléphone saisi via [PhoneInput.tsx](apps/web/src/components/PhoneInput.tsx) (indicatif pays, E.164). Config IA (dont `preferred_engine` : `'openai' | 'gemini'`, **forcé à `gemini` par l'UI** (config IA + wizard — sélecteur de moteur retiré, décision produit Gemini-only ; la colonne et les 2 bridges subsistent, OpenAI testable via `/admin/sante`) ; **voix par moteur** : `ai_voice` (TEXT, défaut résolu `cedar`) = voix OpenAI Realtime, `gemini_voice` (TEXT, `NOT NULL DEFAULT 'Aoede'`) = voix Gemini Live. Chaque moteur garde sa propre voix retenue. Catalogue curé + échantillons audio statiques dans `packages/shared/src/voices.ts` (whitelist DUPLIQUÉE côté edge dans `_shared/callContext.ts` car Deno n'importe pas `packages/shared` → à garder en phase) ; pas de `CHECK` SQL, validation en code avec repli sûr. Échantillons servis depuis `apps/web/public/voice-samples/` (OpenAI `.mp3`, Gemini `.wav`), régénérables via `node apps/web/scripts/make-voice-samples.mjs` (PONCTUEL hors build, requiert `OPENAI_API_KEY` + `GOOGLE_API_KEY`) ; `custom_prompt` TEXT nullable : prompt de personnalité propre au bénéficiaire, copie CONCRÈTE — variables résolues — du défaut, snapshottée à la création par le wizard, éditable dans l'onglet Configuration IA. NULL → fallback sur le défaut `prompt_templates`. **Appels ENTRANTS (cf. Canal 3)** : `inbound_enabled` (BOOL, défaut **false**) = autorise le bénéficiaire à appeler AICOUTE ; `inbound_max_minutes_per_day` (INT, défaut 30) = budget de conversation entrante/24h ; `inbound_cooldown_minutes` (INT, défaut 30) = délai mini entre 2 entrants ; `inbound_max_duration_seconds` (INT, défaut 600) = coupe-circuit durée d'un entrant ; `inbound_custom_prompt` TEXT nullable = ouverture concrète (snapshot wizard, éditable /contexte, NULL → défaut `prompt_templates.inbound_opening`). Tous éditables dans `/contexte` → Config IA → section « Appels entrants ». `report_recipients` TEXT[] (défaut `{}`) : adresses email de proches qui reçoivent l'email de compte-rendu **en plus** de l'aidant, éditable dans la carte « Comptes-rendus envoyés à » de l'onglet **Profil** de `/contexte` ([ProfilTab.tsx](apps/web/src/pages/contexte/tabs/ProfilTab.tsx)). `normalizeRecipients` (`_shared/email.ts`) dédoublonne + valide la liste aidant+proches. `notify_call_report` (BOOL, défaut **true**) : opt-in de l'envoi du **compte-rendu après CHAQUE appel** (`generate-summary`), **maintenant éditable** via l'interrupteur « M'envoyer un compte-rendu après chaque appel » de cette même carte (2026-07-02). **Indépendant** de `session_schedules.notify_on_no_answer` (alerte de NON-RÉPONSE, réglée dans le Planning) → un aidant peut couper les résumés tout en gardant l'alerte de non-réponse (cas d'usage demandé). OFF → aucun email de résumé (ni aidant ni proches), l'alerte non-réponse reste gouvernée par le Planning. **Deux langues distinctes** : `language_preference` (`fr|en|es|de|it`, défaut `fr`) = langue PARLÉE pendant l'appel (résout `{{langue}}`) ; `report_language` (mêmes 5 langues, défaut `fr`) = langue des RETOURS (résumé, alertes, email, page publique `/r/:token`). La **mémoire** (`conversation_memory`) reste, elle, en langue de CONVERSATION car réinjectée dans les appels suivants. Les deux éditées côte à côte dans l'onglet Compagnon IA de `/contexte` + wizard (étape Configuration IA, la 3ᵉ). i18n centralisée et **dupliquée** : `_shared/reportI18n.ts` (edge : libellés catégories/sévérité/humeur + habillage email + locale date) et `apps/web/src/lib/reportI18n.ts` (web : CallDetail + PublicReport) — à garder en phase)
- `session_schedules` — planification récurrente + politique no-answer (`calls_per_week`, `days_of_week`, `time_of_day`, `retry_count`, `retry_interval_minutes`, `notify_on_no_answer`, `no_answer_timeout_seconds`). Contrainte : `array_length(days_of_week) == calls_per_week`. Vue `v_schedules_with_history` ajoute `last_call_at`.
- `calls` — historique des appels + transcript + rapport. Colonnes clés : `attempt_number` (1-4), `notified_at` (origine du timer no-answer + heure effective de déclenchement Twilio), `alerts JSONB` (array d'objets `{category, severity, evidence}` — signaux faibles structurés), `twilio_call_sid` (sid Twilio de l'appel sortant pour idempotence + debug), `engine` (`'openai' | 'gemini' | NULL` — moteur effectif, écrit par initiate-call et confirmé/fallback par le voice-bridge à `markCallInProgress`), `tokens_*` + `ai_cost_eur_real` (snapshot en fin d'appel, tarifs dispatched selon `engine`), `twilio_cost_eur` (coût Twilio RÉEL récupéré async via l'API Twilio par le voice-bridge ; NULL tant que pas remonté → l'UI affiche alors une estimation par la durée). `report_token` + `report_token_expires_at` : jeton de partage public du compte-rendu (page `/r/:token` sans login, expire 48h), ré-émis à chaque envoi d'email par `issueReportToken` (`_shared/reportToken.ts`). `report_language` : **snapshot** de `beneficiaries.report_language` écrit par `generate-summary` au moment de la génération (fige la langue du rapport même si le réglage change ensuite ; NULL sur les appels antérieurs → fallback `fr` à la lecture). `fluidity_metrics JSONB` (nullable) : snapshot technique de **fluidité** écrit par le voice-bridge en fin d'appel (latence de prise de parole « le blanc », barge-ins, faux barge-in/bruit, « allô ? », contexte) — **Étape 0 = observation pure**, aucun réglage auto ; produit par `engines/fluidity.js`, consultable via le CTA « Qualité » de `/admin/appels`. `no_answer_handled_at` (TIMESTAMPTZ nullable, migration `20260629000001`) : marqueur « la politique no-answer a déjà statué sur cet appel » (relance créée OU email final envoyé) — posé par la passe B2 de `schedule-calls` en claim-then-act ; NULL = à arbitrer (cf. « Worker planning + politique no-answer »). `scheduled_at` est immutable après création (= créneau prévu original). `origin` (`'scheduled' | 'inbound' | 'campaign'`, défaut `scheduled`) : distingue un appel SORTANT planifié, un appel ENTRANT (le bénéficiaire a appelé AICOUTE — cf. Canal 3), et un appel de **campagne** org (sortant en masse, `campaign_id` non NULL — cf. « Dashboard ORGANISATION »). Les lignes `inbound` naissent directement `in_progress`, sans `schedule_id` → invisibles pour `schedule-calls` (passes filtrées par statut) et `regenerate-future-calls` (ne touche que les lignes d'un `schedule_id`). Badge « 📞 Entrant » dans `/historique` + `/admin/appels`.
- `conversation_memory` — mémoire long-terme par bénéficiaire (extraite par `generate-summary` après chaque appel). Injectée dans le prompt (top 15 par importance) ET le résumé du **dernier appel terminé** (section « VOTRE DERNIÈRE CONVERSATION »). Consultable + éditable (corriger/ajouter/supprimer) dans l'onglet **Mémoire** de `/contexte` et `/admin/beneficiaires/:id` (hook `useMemories`, CRUD). RLS : aidant (`caregiver_owns_memory`, ALL) + admin (SELECT via `…0003` + INSERT/UPDATE/DELETE via `20260531000001`). **Déduplication à l'extraction (2026-06-15)** : `generate-summary` extrayait les `new_memories` du seul transcript courant, **sans connaître l'existant** → un fait récurrent était ré-inséré à chaque appel (doublons + paraphrases). Fix 2 couches : (1) les mémoires connues sont **injectées dans le prompt d'extraction** (bloc « MÉMOIRES DÉJÀ CONNUES — NE PAS les redire » + règles dédiées : ne renvoyer que des faits réellement nouveaux, quitte à `[]`) ; (2) **filet déterministe à l'insert** = `normalizeMemory(content)` (minuscules, accents `\p{M}` retirés, ponctuation→espace) écarte tout contenu dont la clé existe déjà en base ou se répète dans le lot. ⚠️ N'attrape que les redites quasi-textuelles, **pas les paraphrases sémantiques** (« aime le jardinage » vs « passe du temps au potager ») → la couche 1 les limite à la source mais le rétroactif des paraphrases déjà en base n'est PAS nettoyé (décision 2026-06-15 : rien pour l'instant ; consolidation sémantique LLM = outil à construire si besoin). Note : des mémoires anciennes peuvent être **en anglais** au lieu de la langue de conversation (dérive modèle, non traité).
- `prompts` (2026-06-18) — **BIBLIOTHÈQUE de prompts sélectionnables** (migration `20260618000001`), remplace le défaut UNIQUE `prompt_templates`. **Un prompt = une PAIRE** (les deux vont ensemble). Colonnes : `id` UUID, `title`, `language` (`fr|en|es|de|it` CHECK), `outbound_body` (personnalité + règles, AICOUTE appelle), `inbound_body` (ouverture, le bénéficiaire appelle) — tous deux avec variables `{{persona}} {{prenom}} {{langue}} {{style}} {{il_elle}}`, `is_default` (BOOL), `created_at`/`updated_at`/`updated_by`. **Une seule paire par défaut PAR LANGUE** garantie par l'index unique partiel `prompts_one_default_per_lang` (⚠️ jamais d'`ON CONFLICT` dessus — cf. piège ; la mise en défaut se fait en 2 temps côté Edge). `is_default` = paire présélectionnée dans les menus déroulants (onboarding / fiche / admin) pour la langue ET **fallback côté Edge**. RLS : SELECT `auth.role()='authenticated'` + écriture admin `is_admin()` ; **GRANT** SELECT anon/authenticated + ALL service_role (table créée par migration brute — cf. Bugs connus). **Seed** : migre le contenu de `prompt_templates` → 1 paire `fr` par défaut (`outbound_body`=`template`, `inbound_body`=`inbound_opening`). CRUD admin via l'onglet **Prompts** de `/admin/sante` (table triable titre/langue/date + colonne « Défaut » + actions par ligne **Modifier · Dupliquer · Supprimer** ; Edge Fn `admin-prompts`, service-role, défaut atomique par langue + auto-promotion d'une paire restante à la suppression du défaut). « Dupliquer » crée une paire `« <titre> (copie) »` non-défaut via l'action `create`. `beneficiaries.prompt_id` (UUID FK → `prompts`, `ON DELETE SET NULL`) mémorise la paire source choisie ; les textes concrets restent dans `custom_prompt` (émis) + `inbound_custom_prompt` (entrant), snapshottés depuis la paire et éditables. Menu déroulant partagé [PromptSelect.tsx](apps/web/src/components/PromptSelect.tsx) + hook [usePrompts.ts](apps/web/src/hooks/usePrompts.ts).
- `prompt_templates` — table **singleton** (`id=1`) **LEGACY** : ancien prompt PAR DÉFAUT unique de la plateforme (colonnes `template` + `inbound_opening`). **Plus lu ni écrit** depuis la bibliothèque `prompts` (2026-06-18) : l'éditeur `/admin/prompt` a été retiré (redirige vers `/admin/sante?tab=prompts`), `callContext.ts` lit désormais les défauts de `prompts`, et le wizard / `CompagnonIATab` snapshottent depuis `prompts`. Conservée physiquement (pas de DROP) ; ses seeds (`20260531000002`, `20260608000006`) restent la source du seed initial de `prompts`. Suppression possible plus tard.
- `demo_calls` — tracking des démos vitrine (séparé de `calls`), consultable dans **`/admin/appels` onglet « Démos vitrine »** (anciennement page publique `/track_calls`, retirée le 2026-06-07). Colonne `engine` discrimine OpenAI vs Gemini ; les colonnes tokens et `openai_cost_eur_real` sont réutilisées pour les deux moteurs (sémantique "coût IA réel", quelle que soit l'origine). `fluidity_metrics JSONB` (même forme que `calls.fluidity_metrics`) écrit pour les 3 chemins démo passant par le voice-bridge (tél OpenAI/Gemini + web Gemini ; **pas** le web OpenAI WebRTC, hors boucle serveur) — colonne « Qualité » de l'onglet Démos.
- `system_events` — log structuré (level / source / call_id / message / payload JSONB) écrit par `schedule-calls`, `initiate-call`, voice-bridge. Lu uniquement par les admins via `/admin/sante`. Sert d'historique d'observabilité sans toucher au reste.
- `minute_purchases` (2026-06-09) — crédits de minutes **achetés** (Stripe) ou **crédités via un code** : `caregiver_id`, `pack_id`, `pack_name`, `minutes`, `amount_eur`, `stripe_session_id` (UNIQUE → idempotence du crédit direct), `source_code` (nullable, code ayant crédité), `created_at`. Alimente l'onglet « Mes achats » + le calcul des **minutes disponibles** (`offert essai + acheté + crédits admin − consommé`, hook `useMinutesBalance` ; **conso = appels arrondis à la minute SUPÉRIEURE par appel**). RLS : `caregiver_owns_purchases` (**SELECT only** depuis 2026-06-10 — l'INSERT `authenticated` a été RETIRÉ pour empêcher l'auto-crédit ; écriture = service-role via webhook/redeem) + `admin_select_purchases`. GRANT SELECT `authenticated` + ALL `service_role`. ⚠️ Pas encore de bridage temps réel par solde — `subscriptions`/`PLAN_TIERS` coexistent. Catalogue des packs : `MINUTE_PACKS` dans `packages/shared/src/types.ts` (source UNIQUE vitrine `#tarifs` + back-office).
- `minute_codes` (2026-06-10) — **bons d'achat / codes à usage unique** (achat Stripe INVITÉ, cf. section « Achat de minutes (Stripe) ») : `code` UNIQUE (= le secret), `pack_*`, `minutes`, `amount_eur`, `buyer_email`, `stripe_session_id` UNIQUE (idempotence), `status` active|redeemed, `redeemed_by`, `redeemed_at`. **Jamais exposée au client** (aucun GRANT anon/authenticated → tout accès via Edge en service-role) ; GRANT ALL `service_role`. RLS : `admin_select_minute_codes`. Crédité **atomiquement** par `redeem-code` (UPDATE…WHERE status='active' RETURNING → INSERT `minute_purchases`).
- `minute_adjustments` (2026-06-10) — **crédits manuels admin** (geste commercial / cadeau / test prolongé) : `caregiver_id`, `minutes` (SIGNÉ, >0 crédit), `reason` (motif INTERNE), `created_by` (admin auteur), `created_at`. Écriture via Edge `admin-credit-minutes` (requireAdmin, service-role). Côté aidant affiché en libellé neutre **« Minutes offertes »** (motif masqué). RLS : `caregiver_owns_adjustments` (SELECT) + `admin_select_adjustments` ; GRANT SELECT `authenticated` + ALL `service_role`.
- `signal_actions` (2026-06-18) — **journal de suivi des signaux faibles** (page **admin** `/admin/signaux`, migration `20260618000003`). Une ligne = UNE action menée par l'équipe AICOUTE sur un appel portant ≥1 `calls.alerts` (toutes sévérités). Modèle **append-only** : `call_id` (FK `calls` ON DELETE CASCADE), `status` (`todo|in_progress|done|dismissed` CHECK), `comment`, `author_id` (FK `profiles`, **forcé à `auth.uid()` par la policy INSERT** → impossible de signer au nom d'un autre), `author_name` (SNAPSHOT du nom, reste lisible si le profil change), `created_at`. **Statut courant d'un appel = `status` de son action la plus récente** ; un appel sans aucune action = implicitement « à traiter » (todo). Rien n'est jamais modifié/supprimé (audit qui/quand/quoi). RLS **admin-only** (`admin_select_signal_actions` SELECT + `admin_insert_signal_actions` INSERT `is_admin() AND author_id = auth.uid()`) ; GRANT SELECT+INSERT `authenticated` + ALL `service_role`. Pas d'Edge Fn : écriture directe via client (RLS admin). **Outil INTERNE équipe** : la page aidant `/signaux` est en **lecture seule** et n'y touche pas. **Les signaux eux-mêmes ne sont PAS dupliqués ici** — ils restent dans `calls.alerts` ; cette table ne porte que le suivi.

## Architecture vocale

Deux canaux distincts selon le contexte d'appel — **les deux partagent le même system prompt** construit via `_shared/callContext.ts` (factorisé entre `realtime-token` et `get-call-context` ; `realtime-token` appelle aussi `loadCallContext` depuis le refactor — plus de duplication).

**Construction du prompt** (`_shared/systemPrompt.ts`) : `instructions = [TEMPLATE éditable, interpolé] + [BLOC CONTEXTE assemblé par le code]`.
- **TEMPLATE** = personnalité + règles. Cascade : `beneficiaries.custom_prompt` (déjà concret → tel quel) → **`outbound_body` de la paire par défaut `prompts`** pour la langue du bénéficiaire (`is_default` ; variables résolues par `resolvePromptPlaceholders`) → paire défaut `fr` → `CODE_DEFAULT_TEMPLATE` (filet codé en dur). `loadCallContext` lit la paire défaut `prompts` (par `language_preference`, repli `fr`) + `custom_prompt` et les passe à `buildSystemPrompt(..., defaultTemplate, customPrompt)`. _(Avant 2026-06-18 : `prompt_templates.template` singleton.)_
- **BLOC CONTEXTE** (jamais éditable, `buildContextBlock`) = infos bénéficiaire + `VOTRE DERNIÈRE CONVERSATION` (résumé du dernier appel `completed`) + `CE QUE TU TE RAPPELLES` (mémoire) + sujets suggérés + **durée cible** (spécifique à l'appel).
- **OUVERTURE ENTRANTE** (appels `origin='inbound'`, cf. Canal 3) : `buildSystemPrompt` ajoute EN DERNIER un bloc « OUVERTURE (IMPÉRATIF) » qui prime sur la règle d'ouverture sortante du template. Même cascade que le TEMPLATE : `beneficiaries.inbound_custom_prompt` (concret) → `inbound_body` de la paire défaut `prompts` (langue) → paire défaut `fr` → `CODE_DEFAULT_INBOUND_OPENING` (filet edge). La personnalité (TEMPLATE) reste partagée entre sortant et entrant.
- ⚠️ `resolvePromptPlaceholders` existe en DOUBLE : `packages/shared/promptTemplate.ts` (web : snapshot wizard + sélection d'un prompt) et `systemPrompt.ts` (edge) car Deno n'importe pas `packages/shared`. Le texte du défaut existe désormais dans la table `prompts` (source sélectionnable, seedée depuis l'ancien `prompt_templates`) + un filet codé en dur en DOUBLE par type : `CODE_DEFAULT_TEMPLATE`/`CODE_DEFAULT_INBOUND_OPENING` (edge) et `DEFAULT_PROMPT_TEMPLATE`/`DEFAULT_INBOUND_OPENING` (shared, snapshot wizard si la lecture `prompts` échoue). À garder en phase.

### Canal 1 — Back-office WebRTC direct (simulation aidant)
Utilisé quand l'aidant simule un appel depuis `app.aicoute.fr` (test du contexte / debug). WebRTC direct navigateur ↔ OpenAI.
1. `realtime-token` (Edge Fn) — construit le system prompt via `loadCallContext`, génère un **ephemeral token** GA via `POST /v1/realtime/client_secrets` (token dans `response.value`), passe le call `in_progress`. Le prompt n'est jamais exposé au client.
2. Client — `packages/shared/src/realtime.ts` (`RealtimeSession`) : `getUserMedia` → `RTCPeerConnection` → SDP offer vers `POST /v1/realtime/calls?model=gpt-realtime-2` → data channel `oai-events` (events GA + transcription `whisper-1`).
3. `save-transcript` (Edge Fn) — à la fin, persiste `calls.transcript` puis **await** `generate-summary` (cf. Bugs connus : EdgeRuntime.waitUntil instable).

### Canal 2 — Appels planifiés Twilio (production)
Utilisé pour les vrais appels vers le bénéficiaire, déclenchés par le worker [schedule-calls](supabase/functions/schedule-calls/index.ts). Le bénéficiaire reçoit un appel sur son téléphone (numéro `beneficiaries.phone`), pas de mobile app nécessaire.

1. **[initiate-call](supabase/functions/initiate-call/index.ts)** — lit `beneficiary.preferred_engine` puis POST `${VOICE_BRIDGE_URL}/scheduled-call` avec `Authorization: Bearer ${MODECT_INTERNAL_TOKEN}`, body `{ call_id, phone, engine }`. Si OK → marque le call `notified` + `notified_at` + `twilio_call_sid` + `engine`. Si KO → `failed`.
2. **[voice-bridge `/scheduled-call`](services/voice-bridge/src/server.js)** — auth token interne, refuse `engine='gemini'` en 503 si `GOOGLE_API_KEY` absente, crée l'appel Twilio sortant avec `timeout=SCHEDULED_RING_TIMEOUT` (30 s) vers TwiML `/scheduled-outgoing` qui ouvre une WS `/scheduled-media-stream` (avec `<Parameter name="call_id">` + `<Parameter name="engine">`).
3. **Bénéficiaire décroche** → la WS Twilio démarre :
   - `markCallInProgress(call_id, engine)` côté Supabase (status='in_progress', started_at=now, engine effectif)
   - Dispatch selon engine — fallback automatique vers OpenAI si gemini demandé mais la clé est absente entre-temps :
     - **OpenAI** → [modect-call-bridge.js](services/voice-bridge/src/engines/modect-call-bridge.js) (µ-law direct, events `response.output_audio_transcript.*` + `conversation.item.input_audio_transcription.completed`)
     - **Gemini** → [modect-gemini-bridge.js](services/voice-bridge/src/engines/modect-gemini-bridge.js) (audio converti µ-law ↔ PCM via `engines/audio.js`, events `serverContent.outputTranscription`/`inputTranscription`, voix = `ctx.gemini_voice` du bénéficiaire avec fallback env `GEMINI_VOICE`/`Aoede`)
   - Les deux fetchent le contexte via `get-call-context` (Edge Fn protégée par `MODECT_INTERNAL_TOKEN`, jamais publique) — le prompt est partagé via `_shared/callContext.ts`.
4. **Raccrochage** — `flushFinal()` appelle `save-transcript` (qui chaîne `generate-summary` en arrière-plan) puis `recordCallTokens` écrit `ai_cost_eur_real` + tokens directement dans `calls`.
5. **No-answer / busy / failed** — Twilio notifie le voice-bridge via `POST /scheduled-status` (déclaré comme `statusCallback`), qui marque le call `missed` (no-answer/busy) ou `failed` (failed/canceled) en quelques secondes (≪ que le timeout `no_answer_timeout_seconds`, 120s par défaut). ⚠️ `markCallByTwilioStatus` **ne crée PAS la relance** : la politique no-answer (relance + email final) est centralisée dans la **passe B2** de `schedule-calls`, qui agit sur les `missed`/`failed` non traités quel que soit qui les a marqués (cf. « Worker planning + politique no-answer »). C'est pour ça que ce webhook ne « court-circuite » plus la relance : il marque vite, B2 arbitre ensuite.
6. **Répondeur (AMD)** — l'appel sortant `/scheduled-call` active la détection de répondeur Twilio en mode **asynchrone** (`machineDetection:'Enable'` + `asyncAmd:'true'`, callback `POST /scheduled-amd`). asyncAmd = on NE bloque PAS le TwiML → pour un humain le bonjour part immédiatement (latence nulle). Le verdict `AnsweredBy` arrive ~3-6 s après le décrochage : si c'est une **machine** (`machine_*`) ou un **fax**, `/scheduled-amd` (a) signale au handler WS d'AVORTER via le registre `aicouteCallsBySid` (le `close` WS skip alors le flush → pas de `save-transcript`/résumé), (b) marque le call `missed` (`markCallByTwilioStatus(sid,'no-answer')` → politique no-answer/retry), (c) raccroche l'appel Twilio. On **n'avorte PAS sur `unknown`** (AMD a hésité → risque de raccrocher au nez d'une personne âgée qui répond doucement). **Pourquoi** : sans AMD, un répondeur était traité comme un humain — l'IA parlait dans le vide, produisait un transcript ne contenant que son bonjour, et `generate-summary` **hallucinait** un faux compte-rendu rassurant à partir du seul contexte (mémoire/dernier appel). Double filet côté Edge : `generate-summary` refuse désormais d'analyser un transcript **sans aucun tour `user`** (résumé neutre, **pas d'email**).
7. **Durée fiable** — `calls.duration_seconds` est d'abord écrit par le bridge (temps d'ouverture de la WS Twilio, qui peut sur-compter : répondeur gardant la ligne, WS qui traîne), **puis écrasé par la durée AUTHENTIQUE Twilio** (`call.duration` de la ressource Twilio, secondes de connexion réelles) dans `captureTwilioCost` — ce poller tourne ~15 s APRÈS le flush → il gagne la course de façon déterministe. C'est pourquoi un appel pouvait afficher 2 min (WS) alors que l'audio réel faisait 10 s.

Modèle GA imposé : tout modèle Beta/legacy (`*-realtime-preview`) est ramené à `gpt-realtime-2` par `loadCallContext`. Voix par défaut `cedar`. **Coupure DURE de durée** : `initiate-call` lit `session_schedules.max_duration_minutes` et le passe à `/scheduled-call` (`max_duration_minutes`) → `/scheduled-outgoing` émet `<Parameter name="max_seconds">` = `min(minutes*60 + SCHEDULED_DURATION_GRACE_SECONDS, MAX_SCHEDULED_CALL_SECONDS)` → le handler WS resserre le `safetyTimer` à cette valeur (même mécanisme que les entrants). La durée du planning est donc une **vraie limite** (la cible douce du prompt ne suffisait pas : un appel 5 min resté ouvert courait jusqu'au filet global 900 s = 15 min). Repli sur `MAX_SCHEDULED_CALL_SECONDS` si aucune durée n'est transmise.

### Canal 3 — Appels ENTRANTS Twilio (le bénéficiaire appelle AICOUTE)
Symétrique du Canal 2 mais **amorcé par le bénéficiaire** : il compose le numéro AICOUTE (`+33 9 39 03 52 69`), est reconnu à son numéro, et l'IA décroche avec tout son contexte. **Réutilise ~toute la plomberie du Canal 2** (get-call-context, bridges, flush → save-transcript → generate-summary → email aidant). L'aidant **autorise** ce canal par bénéficiaire (`beneficiaries.inbound_enabled`, défaut OFF) ; les garde-fous de coût passent AVANT le câblage. Activable en self-service dans `/contexte` → Configuration IA → section « Appels entrants » (+ `/admin/beneficiaires/:id`).

1. **Config Twilio** : sur le numéro, « Voice → A CALL COMES IN » = `POST {VOICE_BRIDGE_URL}/inbound-voice` (HTTP POST).
2. **[voice-bridge `/inbound-voice`](services/voice-bridge/src/server.js)** : Twilio POSTe `From`/`To`/`CallSid`. Pipeline → réponse TwiML :
   - **rate-limit** `LIMITS.perInbound` (6/h par numéro source, `rateLimit.js`) AVANT toute requête DB ;
   - **identification** ([`persistence/inbound-call.js`](services/voice-bridge/src/persistence/inbound-call.js) → `findBeneficiaryForInbound`) : `normalizePhone(From)` (strip non `[\d+]` → gère le piège Unicode invisible) comparé aux `beneficiaries` `inbound_enabled=true` UNIQUEMENT (ensemble réduit, match normalisé en JS) ;
   - **garde-fous** (`evaluateInboundQuota`) : cooldown (`inbound_cooldown_minutes`) + budget `inbound_max_minutes_per_day` = SUM(`duration_seconds`) des entrants sur 24h (index partiel `idx_calls_inbound_quota`). En cas de doute → refuse ;
   - **réponse** : inconnu/désactivé → `<Reject>` (ne décroche pas → coût ~0) ; connu mais quota/cooldown → `<Say fr-FR>` court + `<Hangup>` ; OK → `createInboundCall` (ligne `calls` `origin='inbound'`, `in_progress`, `engine` = `preferred_engine` avec repli OpenAI si Gemini non configuré) puis TwiML `<Connect><Stream url=…/inbound-media-stream>` avec `<Parameter>` `call_id` + `engine` + `max_seconds` (= `inbound_max_duration_seconds`). Log `system_events` source `voice-bridge/inbound`.
3. **[voice-bridge `/inbound-media-stream`](services/voice-bridge/src/server.js)** : `wssInbound` → **fonction partagée `handleAicouteCallConnection`** (mutualisée avec `wssScheduled`, label `inbound` ; coupe-circuit de durée resserré au `max_seconds` par bénéficiaire). Le reste est identique au Canal 2 (markCallInProgress idempotent, dispatch bridge, flush final).
4. **Ouverture dédiée** : pour `origin='inbound'`, `buildSystemPrompt` ([`_shared/systemPrompt.ts`](supabase/functions/_shared/systemPrompt.ts)) ajoute EN DERNIER un bloc « OUVERTURE DE CET APPEL (IMPÉRATIF) » qui **prime** sur la règle d'ouverture sortante du template (« {{prenom}}, quel plaisir de t'entendre ! » au lieu de « c'est moi qui t'appelle »). La personnalité reste partagée ; seule l'ouverture change. Cascade identique à `custom_prompt` : `beneficiaries.inbound_custom_prompt` (concret) → `prompt_templates.inbound_opening` (défaut admin) → `CODE_DEFAULT_INBOUND_OPENING` (filet edge). `callContext.ts` lit `call.origin` et aligne aussi la durée cible entrante sur `inbound_max_duration_seconds`.
5. **Email aidant** : identique au sortant (chaîne `save-transcript → generate-summary`), **respecte l'opt-in `notify_call_report`** (pas de cas particulier pour l'entrant).
6. **Sécurité** : la signature `X-Twilio-Signature` n'est PAS vérifiée (exposition bornée par les garde-fous : seul un numéro opt-in connu déclenche une session ; quota + cooldown + durée max + concurrence). Durcissement possible via `twilio.validateRequest`.

> **Contrôle d'admission (concurrence)** — [`services/voice-bridge/src/concurrency.js`](services/voice-bridge/src/concurrency.js) : `acquireCallSlot`/`releaseCallSlot` câblés dans les **3 handlers WS voix** (démo tél, démo web, appels AICOUTE planifiés+entrants). Env `MAX_CONCURRENT_CALLS` (défaut **0 = illimité/désarmé** : ne refuse personne par surprise). La **mesure du pic tourne toujours** → log `system_events` source `voice-bridge/admission` au franchissement de chaque palier (visible `/admin/sante`). Armer à une valeur réelle (ex. 20) une fois le plafond de l'instance Render connu. Le voice-bridge est un process Node unique par instance ; le scaling horizontal est possible (chaque appel est autonome via `call_id`/`engine` en `<Parameter>`), le vrai plafond étant les quotas de concurrence OpenAI/Gemini.

> **⏰ Veille modèles voix — À REFAIRE PÉRIODIQUEMENT (~tous les 1–2 mois).** La **qualité de la conversation est le cœur du produit** et les modèles audio temps réel progressent vite. Vérifier régulièrement (doc/​changelog OpenAI Realtime + Gemini Live, recherche web) si un modèle **plus récent ou meilleur** est sorti, et le **tester avant bascule**. Migration sans douleur : Gemini via l'env `GEMINI_MODEL` (cf. Bugs connus), OpenAI via le label/snapshot dans `_shared/callContext.ts` + bridges voice-bridge.
> **Bouton self-service** : `/admin/sante` → section « Veille modèles voix » → CTA « Lancer la veille » appelle l'Edge Fn `model-watch` (OpenAI Responses API + outil `web_search`, réservée admin via `requireAdmin`) qui compare les modèles EN SERVICE (baseline DUPLIQUÉE dans `model-watch/index.ts` — à garder en phase avec ce doc) à ce qui existe sur le web et renvoie un verdict structuré + sources. Résultat journalisé dans `system_events` (source `model-watch`) → réaffiché au chargement de la page. Modèle de recherche overridable via l'env `MODEL_WATCH_MODEL` (défaut `gpt-4.1`).
> - **État vérifié le 2026-06-05 : on est à jour.** OpenAI `gpt-realtime-2` (snapshot 2026-05-07, dernier généraliste temps réel) ; Gemini `models/gemini-3.1-flash-live-preview` (sorti 2026-03-26, dernier ; toujours *Preview*, pas de GA). Voix actuelles : cedar/marin (OpenAI), Aoede (Gemini).
> - **Levier qualité hors version** : `gpt-realtime-2` expose un `reasoning_effort` configurable (minimal→very high, **défaut `low`**) + contexte 128k. Tester `medium` pour des réponses plus pertinentes (léger surcoût de latence) — non câblé pour l'instant.

### Fluidité de la conversation (VAD / tour de parole)

La fluidité est un axe produit prioritaire. Trois symptômes traités, chacun via la **détection d'activité vocale (VAD)** — orthogonaux : début de parole = barge-in/bruit ; fin de parole = « blanc ».

**① Barge-in trop nerveux (Gemini) — adoucissement.** Par défaut, Gemini Live coupe la voix de l'IA « au couteau » dès qu'il détecte un son entrant. Deux leviers (**ciblés Gemini** ; OpenAI gère l'interruption via `conversation.item.truncate`) :
- **VAD moins nerveuse** ([`engines/vad.js`](services/voice-bridge/src/engines/vad.js)) — helper partagé qui injecte `setup.realtimeInputConfig.automaticActivityDetection` dans les **3** bridges Gemini. Défauts : `startOfSpeechSensitivity = START_SENSITIVITY_LOW` + `prefixPaddingMs = 300` (cf. ③ bruit).
- **Fade-out web** ([`packages/shared/src/geminiLive.ts`](packages/shared/src/geminiLive.ts)) — à l'interruption, un `GainNode` maître fait une rampe 100 %→0 en ~120 ms avant de couper les sources. **Démo navigateur uniquement** (le tél coupe via `event:'clear'` Twilio, audio déjà bufferisé donc non « fondable »).

**② Le « blanc » (silence trop long avant que l'IA réponde)** = détection de FIN de tour.
- **OpenAI** ([`engines/openai-vad.js`](services/voice-bridge/src/engines/openai-vad.js)) — défaut **`semantic_vad`** (`eagerness=high` depuis 2026-06-07, prise de parole la plus rapide après une phrase finie ; repli `medium` par env si l'IA coupe des pauses) : un modèle décide quand l'utilisateur a VRAIMENT fini (selon ses mots), pas un délai de silence fixe → répond vite sur une phrase finie, sans couper une pause de réflexion (idéal personnes âgées). Appliqué aux 2 bridges OpenAI voice-bridge (`modect-call-bridge.js` prod + `openai-bridge.js` démo tél) ET au web WebRTC ([`realtime.ts`](packages/shared/src/realtime.ts), hardcodé `medium` car bundle navigateur sans env — **non aligné sur `high`**, couvre démo web + simulation aidant).
- **Gemini** — pas de fin-de-tour sémantique : seuls `endOfSpeechSensitivity` / `silenceDurationMs` jouent. Laissés au défaut Gemini (les durcir risque de couper une pause) → à régler à l'oreille par env.
- **⚠️ CONCLUSION (investigation 2026-06-08, vérité terrain Audacity sur enregistrements dual-channel) : le « blanc » Gemini téléphone ≈ 2 s est de la LATENCE DE GÉNÉRATION, pas de la VAD.** Mesures validées à l'œil (~1,6-2,35 s) = pile ce que l'instrument `fluidity.js` reporte (détecteur d'énergie `endpointing.js` confirmé fiable). Leviers testés SANS effet : durcir la VAD (`END_SENSITIVITY_HIGH` + `silence=500`), `noise_reduction`, et **désactiver le « thinking »** (`GEMINI_THINKING_BUDGET=0`, cf. `engines/generation.js` — opt-in env, OFF par défaut). Aucun ne réduit le 2 s → c'est le temps intrinsèque de Gemini Live pour produire le 1er son. **Décision produit : ON ACCEPTE le ~2 s** (Gemini gardé pour accent + coût, pause posée OK pour personnes âgées) ; surveiller via la veille modèles si un Live plus rapide sort. Le plan « VAD manuelle » (déclencher la fin de tour côté bridge) a été écarté : gain ~150 ms pour un risque réel de couper une personne âgée. OpenAI testé = pas plus rapide en pratique (≥2-3 s sur les tests).

**③ Bruit d'ambiance pris pour un coupage de parole** = détection de DÉBUT de tour.
- **OpenAI** — **`noise_reduction`** (défaut `far_field`, env) : OpenAI filtre le bruit AVANT la VAD. `far_field` = haut-parleur/pièce (cas fréquent), `near_field` = combiné à l'oreille. En `server_vad`, `threshold` ↑ = exige une voix plus franche.
- **Gemini** — pas de filtre dédié ; `prefixPaddingMs = 300` (vs 200 avant) exige une parole soutenue avant interruption → rejette les bruits brefs.

**④ Le « blanc au démarrage » (silence après le décrochage) — bonjour PROACTIF (≠ VAD).** Le `blank.start_ms` (cf. fluidité) n'est PAS du VAD : c'est le silence entendu juste après le décrochage, avant le 1er mot de l'IA. **Défaut (validé à l'oreille le 2026-06-07) : l'IA salue dès que le setup est prêt, sans attendre.** Le contenu du bonjour vient de la règle n°1 du prompt / `firstMessage` (`firstMessageHint` côté appels planifiés) — pas de « dès que tu entends allô » dans le prompt (le timing est géré en code). Câblé dans les **5 bridges** (appels planifiés [`modect-call-bridge.js`](services/voice-bridge/src/engines/modect-call-bridge.js) + [`modect-gemini-bridge.js`](services/voice-bridge/src/engines/modect-gemini-bridge.js) ET démos [`openai-bridge.js`](services/voice-bridge/src/engines/openai-bridge.js) tél + [`gemini-bridge.js`](services/voice-bridge/src/engines/gemini-bridge.js) tél + [`gemini-bridge-web.js`](services/voice-bridge/src/engines/gemini-bridge-web.js) navigateur) via la constante PARTAGÉE [`engines/greeting.js`](services/voice-bridge/src/engines/greeting.js).
> **PROTECTION DU BONJOUR — « porte micro » (`micGateOpen`).** Problème vécu : l'IA salue, l'interlocuteur dit « allô » par réflexe **par-dessus** → ce « allô » est pris pour un barge-in, coupe le bonjour, et l'IA met ~2 s à reprendre (re-détection de fin de tour sur un mot court). Fix engine-agnostique **côté bridge** : tant que le bonjour d'ouverture n'est pas fini, on **ne transmet PAS** le micro de l'interlocuteur au moteur (on DROP les frames audio) → le moteur n'« entend » pas le « allô », ne s'interrompt pas, le bonjour se déroule en entier. La porte se rouvre dès le **1er `turnComplete` / `response.done`** (filet de sécurité `GREETING_PROTECT_MAX_MS`, défaut 8000, si l'event manquait) → barge-in normal pour tout le reste de l'appel. ⚠️ Conséquence voulue : ce que dit l'interlocuteur *pendant* le bonjour est ignoré (un « allô » réflexe — c'est le but). **Implémenté sur les 5 bridges voice-bridge** : Gemini (`modect-gemini-bridge.js`, `gemini-bridge.js`, `gemini-bridge-web.js`) — porte ouverte au 1er `turnComplete` ; OpenAI (`modect-call-bridge.js`, `openai-bridge.js`) — porte ouverte au 1er `response.done`. (OpenAI a aussi le levier `turn_detection.interrupt_response:false` mais la porte micro est plus simple et uniforme entre moteurs.) Validé à l'oreille sur Gemini le 2026-06-07. **Exception démo navigateur OpenAI** (`realtime.ts`, WebRTC direct) : hors voice-bridge, pas de porte micro. 
> **Mode hybride « attendre le allô » (`GREETING_FALLBACK_MS > 0`) — testé et ABANDONNÉ.** On laissait l'interlocuteur parler en premier ; mais sur un mot court (« allô ? ») le `semantic_vad` hésite → blanc interminable. Défaut `0` = proactif. La porte micro rend ce mode encore plus défunt (la porte fermée empêche la détection de l'« allô ») → conservé uniquement comme délai configurable avant le bonjour. **Exception démo navigateur OpenAI** ([`realtime.ts`](packages/shared/src/realtime.ts), WebRTC direct, partagée avec la simulation aidant) : proactif immédiat, ni porte micro ni hybride (différé).

**Fine-tuning depuis le back-office (2026-06-11)** — les seuils d'analyse WAV + la VAD live Gemini/OpenAI sont éditables dans **`/admin/sante` → onglet « Fine-tuning »** ([AdminSante.tsx](apps/web/src/pages/admin/AdminSante.tsx), `FineTuningSection`), **sans redémarrer Render**. Stockés dans `app_settings.fluidity_tuning` (JSONB des seules clés surchargées, migration `20260611000003`). Le voice-bridge résout en **cascade DB → env → défaut** via [persistence/tuning.js](services/voice-bridge/src/persistence/tuning.js) (`TUNING_DEFS` = source de vérité + bornes/clamp ; **miroir UI à garder en phase**) et rafraîchit un **cache en mémoire toutes les ~15 s** (`startTuningRefresh`, démarré au boot). `vad.js`/`openai-vad.js`/`wav-analysis.js` lisent `getTuning()` **au moment de l'appel** (plus de const figée au chargement). Une valeur saisie dans l'UI prime toujours ; champ vide → env Render (si définie) → défaut. Les env ci-dessous restent un filet/escape hatch. (Greeting `GREETING_*` reste env-only pour l'instant : consts importées par 5 bridges.)
**Snapshot par appel (`tuning_snapshot`)** — pour le calibrage, on fige `getTuning()` au **DÉMARRAGE** de chaque appel (pas à la fin : sinon un changement de réglage juste après le raccrochage serait attribué à tort à l'appel) dans la colonne JSONB `calls.tuning_snapshot` / `demo_calls.tuning_snapshot` (migration `20260611000004`), écrite par `markCallInProgress` (appels) et `recordDemoStart` (démos). Le modal « Qualité » l'affiche en section « Réglages de cet appel (conversation) » = VAD live du moteur utilisé. Les seuils d'**analyse WAV**, eux, restent dans `recording_analysis.vad` (figés au moment de l'analyse, re-figés si ré-analyse).

**Réglages env (voice-bridge / Render)** — surchargeables sans redéploiement (kill-switch côté chaque moteur) ; doublonnés par l'onglet Fine-tuning ci-dessus (DB prime) :
- **Gemini** : `GEMINI_VAD_DISABLED` · `GEMINI_VAD_START_SENSITIVITY` (défaut `START_SENSITIVITY_LOW`) · `GEMINI_VAD_PREFIX_PADDING_MS` (défaut `300`) · `GEMINI_VAD_END_SENSITIVITY` / `GEMINI_VAD_SILENCE_DURATION_MS` (anti-« blanc », non envoyés par défaut).
- **OpenAI** : `OPENAI_VAD_DISABLED` · `OPENAI_VAD_TYPE` (défaut `semantic_vad`, ou `server_vad`) · `OPENAI_VAD_EAGERNESS` (défaut `high`) · `OPENAI_NOISE_REDUCTION` (défaut `far_field`, ou `near_field`/`off`) · `OPENAI_VAD_THRESHOLD`/`_PREFIX_PADDING_MS`/`_SILENCE_DURATION_MS` (server_vad only).
- **Démarrage (tous les bridges voice-bridge : appels planifiés + démos tél/web Gemini)** : `GREETING_FALLBACK_MS` (**défaut `0` = bonjour proactif immédiat**, validé à l'oreille). `> 0` = mode hybride « attendre le allô » (abandonné, cf. ④). Sauf démo navigateur OpenAI (WebRTC direct, hors voice-bridge).

⚠️ `realtimeInputConfig` (Gemini) et `audio.input.{turn_detection,noise_reduction}` (OpenAI) sont des champs valides et tout reste optionnel (kill-switch) → aucun risque de setup malformé. Barge-in Gemini validé en prod le 2026-06-01. `semantic_vad` + `noise_reduction` + prefix 300 = **à valider à l'oreille** (caveat OpenAI : un retour communautaire signale parfois +latence avec noise_reduction → togglable).

### Observabilité fluidité (Étape 0 — observation pure)
Avant tout réglage automatique, on **mesure**. Un tracker engine-agnostique [`engines/fluidity.js`](services/voice-bridge/src/engines/fluidity.js) accumule pendant l'appel des signaux bruts et écrit un snapshot agrégé en fin d'appel :
- **`calls.fluidity_metrics`** (appels planifiés OpenAI + Gemini) via `recordCallFluidity` ; **`demo_calls.fluidity_metrics`** (démos tél OpenAI/Gemini + web Gemini) via `recordDemoEnd`/`recordDemoRealCost`. Câblé dans les **5 bridges** (`getFluidityMetrics(durationSeconds)`). Pas le web OpenAI WebRTC (hors boucle voice-bridge — différé).
- Contenu : `blank` (latence prise de parole : `start_ms` + `turn_avg/p90/max_ms` + `samples_ms` bruts ; **précis pour OpenAI** via `speech_stopped` ET **pour Gemini TÉLÉPHONE** via une ancre acoustique de fin de parole — détecteur d'énergie local [`engines/endpointing.js`](services/voice-bridge/src/engines/endpointing.js) qui écoute l'audio entrant µ-law et appelle `fluidity.onUserSpeechStop(at)`, **lecture seule** sans toucher au flux Gemini ni à sa VAD ; **`approx=true` ne reste que pour Gemini SANS ancre** = démo web ou kill-switch `GEMINI_ENDPOINT_DISABLED` → proxy transcript qui **SOUS-ESTIME** le « blanc » car la transcription Gemini arrive en retard, collée à la réponse IA. Pourquoi cette ancre : sans elle, /admin/qualite affichait un « blanc » Gemini ~64 ms — artefact, pas la latence perçue. Réglages env endpoint : `GEMINI_ENDPOINT_HANG_MS` (défaut 350, silence→fin de tour) · `GEMINI_ENDPOINT_ONSET_MS` (80) · `GEMINI_ENDPOINT_MIN_RMS` (500). **`blank` ne compte QUE les tours de parole propres** : un gap qui suit immédiatement un barge-in est EXCLU du blanc et rangé dans `barge_in.recovery_*` — sinon la latence d'abandon+régénération après interruption gonflait le blanc, ce qui produisait des max irréalistes ~4 s), `barge_in` (`total`/`per_min`/`suspected_false` = barge-in non suivi de parole = bruit probable ; `recovery_avg/p90/max_ms` + `recovery_samples` = latence de reprise après interruption, mesurée À PART du blanc), `presence_checks` (« allô ? » regex multilingue, null si pas de transcript), `turns` + `assistant_speech_ms` + `speech_ratio`.
- Lecture : CTA « **Qualité** » par appel → modal partagé [`components/FluidityModal.tsx`](apps/web/src/components/FluidityModal.tsx), dans `/admin/appels` onglets passés (si métriques présentes) et Démos vitrine. Aucun ajustement auto — on analyse les chiffres à la main pour décider de l'étape 1 (profil de fluidité par bénéficiaire).
- **`blank` ne compte QUE les tours de parole propres** : un gap suivant immédiatement un barge-in est exclu et rangé dans `barge_in.recovery_*` (sinon la latence de reprise post-interruption gonflait le « blanc » → max irréalistes ~4-6 s). Ajouté le 2026-06-08.

### Enregistrement WAV systématique des appels (phase de TEST — `RECORD_ALL_CALLS`)
**État (2026-06-11)** : aicoute.fr est en TEST. Pour analyser la qualité des conversations, **TOUS les appels sont enregistrés** en WAV dual-channel (canal 1 = interlocuteur, canal 2 = IA) — démos vitrine + appels planifiés sortants + appels entrants — tant que l'env voice-bridge `RECORD_ALL_CALLS != "0"` (défaut ON). Migration `20260611000001` : colonnes `calls.recording_path` + `demo_calls.recording_path` + `demo_calls.twilio_call_sid` (corrélation) + policy storage `admin_read_fluidity_recordings` (lecture admin du bucket privé → lien signé minté à la volée). Flux : recording armé via REST `record/recordingChannels=dual` (sortants : `/call`, `/scheduled-call`) ou verbe TwiML `<Start><Recording>` (entrant : `/inbound-voice`) → Twilio POST `/recording-status` → le bridge télécharge le WAV, le dépose dans le bucket `fluidity-recordings` (chemin `calls/{CallSid}.wav`), supprime la copie Twilio, **rattache le chemin à la ligne d'appel** via le CallSid (`attachRecordingPath` : essaie `calls` puis `demo_calls`) ET journalise un lien signé 7 j dans `system_events`. **Lecture** : bouton « .wav » par appel ([RecordingButton.tsx](apps/web/src/components/RecordingButton.tsx), mint un lien signé 1 h via `createSignedUrl`) dans `/admin/appels` onglets **passés / émis / Démos vitrine**. ⚠️ **RGPD** : couvre désormais les **vrais appels bénéficiaires** (données de santé) — override assumé pour la phase de test fermée ; repenser le périmètre (ou `RECORD_ALL_CALLS=0`) avant la prod. Coût : Twilio facture l'enregistrement (~$0.0025/min) ; la copie Twilio est supprimée après download (stockage chez nous).

#### Analyse de fluidité « vérité terrain » sur le WAV (remplace le live)
Le « blanc » mesuré **en temps réel** ([engines/fluidity.js](services/voice-bridge/src/engines/fluidity.js) + [endpointing.js](services/voice-bridge/src/engines/endpointing.js)) s'est avéré **infidèle** (un seul canal mélangé, seuils approximatifs, retard de détection) — il surestime/déforme la latence (cas vécu : 3,0 s moy / 5,5 s max là où l'écoute Audacity montrait bien moins). Solution : une **analyse OFFLINE du WAV dual-channel** = l'automatisation de la mesure à l'œil dans Audacity. [engines/wav-analysis.js](services/voice-bridge/src/engines/wav-analysis.js) (`analyzeDualChannelWav`) parse le WAV PCM16 (déjà en mémoire dans `/recording-status`, ~100 ms de calcul), fait une **VAD énergie par canal** (RMS par frames de 20 ms + seuil = bruit de fond × facteur + hangover ; la **fin de parole est REDATÉE à la dernière frame sonore** → fidèle, contrairement au live qui marque +hang ms trop tard), reconstruit les segments `[début, fin]` par canal, identifie le **canal IA = canal 1 (la jambe SORTANTE Twilio = l'audio qu'on diffuse), DÉTERMINISTE** (override `WAV_AI_CHANNEL`=`0`|`1`). ⚠️ L'auto-détection « 1er à parler / 1er segment soutenu » a été **ABANDONNÉE** (commit `c287a18`) : avec la latence du bonjour (~1 s), l'interlocuteur dit souvent « allô » **AVANT** l'IA (capté par l'enregistrement même si la porte-micro l'a masqué au modèle) → inversait blanc/réactivité (bug 2026-06-11). Le bonjour proactif (1er segment IA) ne compte jamais comme un « blanc » ; `WAV_GREETING_MIN_MS`=400 ne sert plus qu'à dater `start_ms` (ignore un clic résiduel). Puis calcule : le **vrai « blanc »** (fin interlocuteur → début IA, distribution avg/p90/max/min + tous les échantillons), la **réactivité de l'interlocuteur** (fin IA → reprise), le **barge-in** — **coupure réelle** (`interrupted` : l'IA cède, son segment finit avant celui de l'interlocuteur) distinguée du **backchannel/talk-over** (« oui/mmh » par-dessus, l'IA continue) — + chevauchements, tours/temps de parole/ratio. Stocké dans la colonne JSONB **`recording_analysis`** (migration `20260611000002`, sur `calls` ET `demo_calls`) **avec les données brutes** (`segments` + `samples_ms`) pour des stats ultérieures. **Affichage** : [FluidityModal.tsx](apps/web/src/components/FluidityModal.tsx) montre `recording_analysis` (badge « vérité terrain ») **dès qu'elle existe**, et retombe sur `fluidity_metrics` (live, conservé en base) seulement pour les appels sans enregistrement. Seuils VAD surchargeables par env pour **calibrer contre Audacity** : `WAV_FRAME_MS` (20), `WAV_HANG_MS` (250), `WAV_ONSET_MS` (90), `WAV_VAD_FACTOR` (3.5), `WAV_VAD_MIN_RMS` (250), `WAV_AI_CHANNEL` (`0`|`1` pour forcer). ⚠️ **Phase de calibrage** : comparer la sortie aux mesures Audacity sur quelques appels réels et ajuster ces env avant de s'y fier aveuglément.

### Diagnostic fluidité — enregistrement de calibration (`/admin/sante`)
Outil de **vérité terrain** pour valider/calibrer les métriques de fluidité, piloté **sans CLI/env** depuis la section « Diagnostic fluidité » de `/admin/sante` ([AdminSante.tsx](apps/web/src/pages/admin/AdminSante.tsx), composant `FluidityDiagnosticSection`). Périmètre historique **démos + appels test** (les vrais appels sont désormais aussi enregistrés via `RECORD_ALL_CALLS`, cf. ci-dessus). **Note** : depuis l'enregistrement systématique, le compteur `fluidity_keep_recording_remaining` n'arme plus rien (le `/call` enregistre toujours) — les boutons « +1/+3 appels » de `/admin/sante` sont devenus **inertes** (à retirer ou recâbler plus tard).
- **Table `app_settings`** (singleton id=1, migration `20260608000001`) : `fluidity_diagnostic_enabled` (bool, réservé à la phase 2 — voir plus bas) + `fluidity_keep_recording_remaining` (compteur dégressif). RLS admin + GRANTs `authenticated` ET `service_role` (⚠️ les DEUX requis : `…0002` a rattrapé le GRANT service_role oublié → sinon le voice-bridge fait « permission denied » et lit 0). Bucket privé Storage `fluidity-recordings`.
- **Flux** : bouton « +1/+3 appels » → incrémente le compteur. Au prochain `/call` démo, le voice-bridge ([server.js](services/voice-bridge/src/server.js)) lit `app_settings` (via service role), arme l'enregistrement Twilio **dual-channel** (canal 1 = appelé, canal 2 = IA), décrémente. Twilio appelle `POST /recording-status` → le bridge télécharge le WAV, le dépose dans Storage (lien signé 7 j), supprime la copie Twilio, et journalise le lien dans `system_events` (source `voice-bridge/fluidity-diag`) → affiché cliquable dans `/admin/sante`. Ouvrir dans Audacity, mesurer le blanc à l'œil.
- ⚠️ La lecture `system_events` côté back-office a aussi nécessité un GRANT SELECT `authenticated` oublié (`…0003`) — sinon la section « Événements système » ET la liste des enregistrements restaient vides.
- **Phase 2 (analyseur VAD offline en RAM, audio jeté) = ABANDONNÉE** : la vérité terrain a montré que le détecteur live (`endpointing.js`) est déjà fiable → inutile. Le toggle « Analyse fine automatique » (`fluidity_diagnostic_enabled`) reste posé mais **inerte**.

## Back-office aidant (app.aicoute.fr)

SPA React mono-utilisateur. Hypothèse : 95% des aidants n'ont qu'**un seul bénéficiaire**, donc l'architecture est centrée sur **un bénéficiaire sélectionné globalement** (pas de listes navigables).

### Layout commun
- **Sidebar gauche** ([AppLayout.tsx](apps/web/src/components/AppLayout.tsx)) : Tableau de bord / **Bénéficiaires** (route `/contexte`) / **Appels** (route `/historique`) / **Signaux** (route `/signaux`, ex-« Veille » remplacée le 2026-06-18) + section « Mon compte » séparée en bas. (Renommages 2026-06-09 ; **« Planning » retiré du menu** → devenu un onglet de la fiche bénéficiaire.)
- **Header sticky** ([AppHeader.tsx](apps/web/src/components/AppHeader.tsx)) sur toutes les pages : dropdown bénéficiaire (défaut = 1er, persisté localStorage `modect.selected_beneficiary_id`) + bouton « Nouveau bénéficiaire » (**actif** → wizard ; multi-bénéficiaires supporté).
- **Bénéficiaire sélectionné** partagé via [useSelectedBeneficiary.tsx](apps/web/src/hooks/useSelectedBeneficiary.tsx) (React Context provisionné dans AppLayout). Les pages lisent `selected` du context, **pas l'URL**.
- **Largeur unifiée** : toutes les pages back-office (aidant + admin) utilisent `max-w-[1400px] mx-auto px-4 py-8`.

### Fiche bénéficiaire (éditeur partagé refondu — 2026-06-09)
[BeneficiaryContextEditor.tsx](apps/web/src/pages/contexte/BeneficiaryContextEditor.tsx) est l'éditeur **partagé** monté côté aidant (`/contexte` = page « Bénéficiaire ») ET admin (`/admin/beneficiaires/:id`). **Lecture-d'abord + édition inline par carte** (crayon « Modifier », pas de méga-formulaire). Découpé en :
- [cards.tsx](apps/web/src/pages/contexte/cards.tsx) : primitives `EditableCard` (bascule lecture⇄édition, état local par carte ; chaque formulaire d'édition est un **composant enfant** monté à l'édition → Rules of Hooks), `Field`/`Chip`/`InfoRow`/`EditFooter`, hook `useSection` (persistance via `useBeneficiary().update`), constantes `LANGUAGES`/`STYLES`, helpers `computeAge`/`formatBirthDate`.
- `tabs/` : [ProfilTab](apps/web/src/pages/contexte/tabs/ProfilTab.tsx) (**fusion 4-en-1** : Son histoire + Goûts & intérêts [+ encadré rouge « sujets à éviter »] + Personnalité, en grille 3 colonnes ; carte Identité avec date de naissance / genre / [PhoneInput.tsx](apps/web/src/components/PhoneInput.tsx) ; **zone danger « Effacer » côté aidant** via prop `onDeleted`), [CompagnonIATab](apps/web/src/pages/contexte/tabs/CompagnonIATab.tsx) (ex-Config IA : identité+voix, style, prompt, ouverture appels émis, langues, appels émis), [MemoireTab](apps/web/src/pages/contexte/tabs/MemoireTab.tsx), [PlanningTab](apps/web/src/pages/contexte/tabs/PlanningTab.tsx) (**admin** : lecture seule + édition gardée par confirmation), [CaregiverPlanningTab](apps/web/src/pages/contexte/tabs/CaregiverPlanningTab.tsx) (**aidant** : abonnement/essai + édition directe), [AppelsTab](apps/web/src/pages/contexte/tabs/AppelsTab.tsx) (**admin** : bouton **« Provoquer un appel »** = appel sortant de test immédiat — insert `calls` + `initiate-call`, désactivé sans numéro ; graphe minutes/sem reçus·émis ; tableau coûts avec colonne **Actions** par appel — Détail `/historique/:id`, **Qualité** (`FluidityModal`), **.wav** (`RecordingButton`), **Renvoyer le mail** (`resend-report`), **Relancer** (missed/failed sortants) — mêmes actions que `/admin/appels`).
- **Onglets visibles** : Profil · Compagnon IA · Mémoire (toujours) ; **Planning** (`withSchedule` admin OU `withCaregiverPlanning` aidant) ; **Appels** (`withCalls` admin). En-tête identité + 5 vignettes ([BeneficiaryAdminHeader.tsx](apps/web/src/pages/contexte/BeneficiaryAdminHeader.tsx)) + zone danger archive/effacer = **page admin uniquement**.

### Pages
| Route | Rôle | Composant |
|---|---|---|
| `/dashboard` | Vue d'ensemble (cards par bénéficiaire — à retravailler) | [Dashboard.tsx](apps/web/src/pages/dashboard/Dashboard.tsx) |
| `/contexte` | Page **« Bénéficiaire »** (ex-Contexte). Éditeur partagé refondu (cf. ci-dessus) : onglets Profil / Compagnon IA / Mémoire / **Planning** (aidant : abonnement + planning) ; lecture-d'abord, édition inline, zone danger « Effacer » en bas du Profil. `?tab=planning` ouvre direct l'onglet Planning ; `?created=1` affiche la bannière post-wizard. | [ContextePage.tsx](apps/web/src/pages/contexte/ContextePage.tsx) + [BeneficiaryContextEditor.tsx](apps/web/src/pages/contexte/BeneficiaryContextEditor.tsx) |
| `/historique` | Page **« Appels »** (ex-Historique). **3 onglets** : **Appels reçus** (sortants AICOUTE→bénéficiaire) / **Appels émis** (entrants `origin=inbound`) / **Appels prévus** (projection 15 j). | [HistoriquePage.tsx](apps/web/src/pages/historique/HistoriquePage.tsx) |
| `/historique/:id` | Compte-rendu détaillé d'un appel (transcript, alerts structurés en cartes catégorie+gravité+citation) | [CallDetail.tsx](apps/web/src/pages/historique/CallDetail.tsx) |
| `/signaux` | Page **« Signaux »** (aidant, remplace « Veille » le 2026-06-18). Liste **LECTURE SEULE** des signaux faibles (`calls.alerts`) du **bénéficiaire sélectionné dans l'en-tête** (`useSelectedBeneficiary` + `useCalls(selected.id)` → RLS aidant). Colonnes date / type (📞 Émis = `inbound` / Reçu) / signal (badges sévérité colorés + citation) / **Détail** (`/historique/:id`). Filtre **sévérité multi-sélection** (Toutes / Élevée / Modérée / Faible), tri date décroissante. **Pas** de journal d'actions (`signal_actions` admin-only) **ni** de bouton .wav (bucket enregistrements admin-only). Couleurs sévérité alignées sur CallDetail (low=amber, medium=orange, high=rouge). | [SignauxPage.tsx](apps/web/src/pages/signaux/SignauxPage.tsx) |
| `/compte` | **3 onglets** : Mon profil / **Mon solde** (relevé crédit/débit/solde calculé à la volée, hook [useMinuteLedger.ts](apps/web/src/hooks/useMinuteLedger.ts)) / **Mes achats** (acheter un pack → Stripe via `startCheckout`, « J'ai un code » → `redeem-code`, historique) + **carte « Minutes disponibles »** en haut (hook [useMinutesBalance.ts](apps/web/src/hooks/useMinutesBalance.ts), paramétrable par `caregiverId`). Tableaux/carte = **vues partagées** avec l'admin dans [MinutesViews.tsx](apps/web/src/pages/compte/MinutesViews.tsx). | [ComptePage.tsx](apps/web/src/pages/compte/ComptePage.tsx) |
| `/beneficiary/new` | Wizard d'onboarding **3 étapes** (Infos de base / **Sa personnalité** [fusion Histoire+Goûts+Personnalité] / Configuration IA). Date de naissance (`birth_date`), genre Femme/Homme, téléphone `PhoneInput`. | [BeneficiaryWizard.tsx](apps/web/src/pages/beneficiary/BeneficiaryWizard.tsx) |

`/planning` n'est **plus une page** : il redirige vers `/contexte?tab=planning` (cf. Redirections legacy). `ScheduleEditor.tsx` + `PlanChooser.tsx` (sous `pages/planning/`) restent réutilisés par les onglets Planning. Après création via le wizard → redirection `/contexte?tab=planning&created=1`.

### Module administration (`/admin/*`)

Visible uniquement si `profile.role = 'admin'`. Entrée dédiée dans la sidebar (palette `accent` ocre, distincte de la navigation aidant). Garde-fou [RequireAdmin.tsx](apps/web/src/components/RequireAdmin.tsx) + hook [useIsAdmin.ts](apps/web/src/hooks/useIsAdmin.ts).

| Route | Rôle | Composant |
|---|---|---|
| `/admin` | Vue d'ensemble : 3 KPI compactes (Aidants/Bénéficiaires · Appels 24h `passés / à venir` calculés sur `scheduled_at` · Coût IA/Twilio 7j) + section **Activité & coûts** (2 graphes recharts — appels+minutes double axe Y, coûts IA+Twilio — avec sélecteur de fréquence 10j/8sem/6mois) + tableau **Coût moyen par minute** (IA/Twilio/total sur 8j/8sem/6mois). Graphes & tableau alimentés par 1 seul fetch des appels `completed` sur 6 mois, bucketisé côté client. | [AdminDashboard.tsx](apps/web/src/pages/admin/AdminDashboard.tsx) |
| `/admin/comptes` | Liste de tous les profils (aidants + admins) avec nb bénéficiaires, nb calls 30j, dernier appel + lien « Gérer ». **Colonnes triables** (tri par défaut = nom de famille A→Z, asc/desc). | [AdminComptes.tsx](apps/web/src/pages/admin/AdminComptes.tsx) |
| `/admin/comptes/:id` | **Onglets miroir du « Mon compte » aidant** : **Profil** (édition nom/email/tél/fuseau, rôle en lecture seule + bénéficiaires rattachés + zone danger « Supprimer ») / **Son solde** (carte solde + relevé + **CTA « Créditer des minutes »** → Edge `admin-credit-minutes`, motifs rapides + texte libre + historique des crédits avec motif) / **Ses achats**. Hooks `useMinutesBalance`/`useMinuteLedger` paramétrés par `caregiverId` (RLS admin) + vues partagées [MinutesViews.tsx](apps/web/src/pages/compte/MinutesViews.tsx). | [AdminCompteDetail.tsx](apps/web/src/pages/admin/AdminCompteDetail.tsx) |
| `/admin/beneficiaires` | Liste globale + colonne aidant + état `notify_call_report` + alerte si pas de téléphone + lien « Gérer ». **Colonnes triables** (tri par défaut = nom de famille A→Z, asc/desc ; bénéficiaires jamais appelés en bas). | [AdminBeneficiaires.tsx](apps/web/src/pages/admin/AdminBeneficiaires.tsx) |
| `/admin/beneficiaires/:id` | Édition **complète** (réutilise `BeneficiaryContextEditor` refondu — cf. « Fiche bénéficiaire ») : en-tête identité + 5 vignettes (`BeneficiaryAdminHeader`) + onglets Profil / Compagnon IA / Mémoire / **Planning admin** (`withSchedule` : lecture seule, édition déverrouillée après **confirmation** ; `ScheduleEditor` avec `caregiverId` = celui du bénéficiaire) / **Appels** (`withCalls`) + zone danger « Archiver/Réactiver » et « Effacer définitivement » (saisie du nom) au niveau de la page. | [AdminBeneficiaireDetail.tsx](apps/web/src/pages/admin/AdminBeneficiaireDetail.tsx) |
| `/admin/appels` | **4 onglets** : passés / **émis** / prévus / **Démos vitrine**. Le **split par origine** : onglet « passés » = SORTANTS uniquement (`origin != 'inbound'`) ; onglet **« Appels émis »** = ENTRANTS (`origin='inbound'`, le bénéficiaire a appelé — cf. Canal 3), même rendu que « passés » (colonnes Planifié/Effectif + Coût IA + Coût Twilio + CTA Qualité ; pas de « Relancer » qui n'a pas de sens pour un entrant). Variable `pastLike = tab === 'past' || tab === 'emitted'` pilote les colonnes communes. Filtres URL (`?period=`, `?status=`, `?severity=high`). Filtre **Période contextuel** : onglets passé/émis bornent `scheduled_at` à `now − X` (libellés « N derniers jours »), onglet prévus à `now + X`. Actions : « Relancer » missed/failed (passés), « Déclencher maintenant » prévus, « Supprimer » prévus. **Onglet Démos** = logs `demo_calls` (rapatriés de `/track_calls`) lus en direct via la policy `admin_all_demo_calls` (pas de clé) : totaux + table mode/moteur/coûts + CTA Qualité — composant [AdminDemosTab.tsx](apps/web/src/pages/admin/AdminDemosTab.tsx). | [AdminAppels.tsx](apps/web/src/pages/admin/AdminAppels.tsx) |
| `/admin/signaux` | **Boîte de réception des signaux faibles** (2026-06-18). Regroupe **tous** les appels portant ≥1 `calls.alerts` (toutes sévérités), les plus graves mis en avant. But : si on détecte du potentiellement dangereux, **agir** (prévenir l'aidant) et **garder la trace** des actions (qui/quand/quoi). Requête : appels avec alertes non vides via `.neq('alerts', '[]')` (exclut `[]` ET NULL), filtre/tri côté client. ⚠️ Piège historique : `.contains('alerts', [..])` avec un **tableau JS** est cassé (supabase-js sérialise en `[object Object]`) → si besoin d'un containment, passer le JSON en **chaîne**. Colonnes : date effective · type (📞 Émis = `inbound` / Reçu = sortant) · bénéficiaire · signal (badge **catégorie · sévérité** coloré + citation, plusieurs possibles) · aidant · suivi (badge statut + nb actions) · actions (**Détail** `/historique/:id` + **.wav** `RecordingButton`). **Filtres sur une ligne, multi-sélection** : Statut (À traiter/En cours/Traité/Sans suite, défaut À traiter+En cours) + Sévérité (Élevée/Modérée/Faible) ; « Tous »/« Toutes » = set vide = aucun filtre. **Tri par date d'appel décroissante** (sévérité en départage). Compteur « N à traiter ». **Modal « Suivi »** : choix de statut + commentaire → ajoute une entrée `signal_actions` ; historique complet en dessous. Auteur = `useAuth().profile`. Pastilles de sévérité aussi affichées dans `/admin/appels` (faible=amber/moyenne=orange/haute=brique, sévérité max). | [AdminSignaux.tsx](apps/web/src/pages/admin/AdminSignaux.tsx) |
| `/admin/qualite` | **Fusionné (2026-06-18) dans `/admin/sante` (page « Administration »), onglet Qualité** → redirige vers `/admin/sante?tab=qualite`. Le contenu vit dans `QualiteSection` (export de [AdminQualite.tsx](apps/web/src/pages/admin/AdminQualite.tsx)). Stats de fluidité agrégées (temps réel) sur `calls.fluidity_metrics` **ET `demo_calls.fluidity_metrics`** : filtres périmètre (global / aidant / bénéficiaire) + période (8/30 j) + Source (réels / démos / les deux) ; KPIs + **comparaison OpenAI vs Gemini** (« blanc » poolé depuis `samples_ms` bruts ; repère `~` = approx Gemini). | _(redirect → AdminSante)_ |
| `/admin/prompt` | **Retiré (2026-06-18)** → redirige vers `/admin/sante?tab=prompts` (l'édition du prompt par défaut est devenue la bibliothèque `prompts`, onglet Prompts de Santé système). | _(redirect dans App.tsx)_ |
| `/admin/sante` | Page **« Administration »** (titre affiché), **5 onglets dans l'ordre : Prompts · Santé système · Fine-tuning · Veille · Qualité** (sélectionnables via `?tab=prompts\|sante\|tuning\|veille\|qualite`). **Prompts** : CRUD bibliothèque `prompts` (table triable titre/langue/date + recherche + colonne « Défaut » + actions Modifier · Dupliquer · Supprimer ; [PromptsLibrary.tsx](apps/web/src/pages/admin/PromptsLibrary.tsx), Edge `admin-prompts`). **Santé système** : calls bloqués (`notified` > 5 min · `in_progress` > 30 min · `scheduled` retry > 5 min) + dernier appel terminé + auto-refresh 30s (bouton Rafraîchir visible sur cet onglet) + **« Diagnostic fluidité »** + **Événements système** (50 derniers). **Fine-tuning** : seuils WAV + VAD live (`app_settings.fluidity_tuning`). **Veille** : « Veille modèles voix » (Edge `model-watch`) + « Test d'appel OpenAI / Gemini » (via `/call`). **Qualité** : `QualiteSection` (ex-`/admin/qualite`) — stats de fluidité agrégées + comparaison OpenAI/Gemini. | [AdminSante.tsx](apps/web/src/pages/admin/AdminSante.tsx) |

**RLS admin** : la migration `20260529000003_admin_role.sql` ajoute une fonction `is_admin()` (SECURITY DEFINER + STABLE) et des policies additives `admin_all_*` qui ouvrent SELECT (et UPDATE/INSERT sur `calls`) à tout admin. Les policies caregiver existantes ne sont pas touchées — les deux jeux sont OU-isés par Postgres. La migration `20260529000010_admin_edit_delete.sql` ajoute en plus les policies `admin_update_beneficiaries` / `admin_delete_beneficiaries` et passe `calls.beneficiary_id` en `ON DELETE CASCADE` (sinon l'effacement définitif d'un bénéficiaire ayant des appels échouerait sur la FK). La migration `20260529000011_admin_delete_calls.sql` ajoute `admin_all_calls_delete` (suppression d'un appel prévu depuis `/admin/appels`). La migration `20260601000001_admin_edit_session_schedules.sql` ajoute `admin_insert/update/delete_session_schedules` (édition du planning depuis l'onglet Planning de `/admin/beneficiaires/:id` — l'admin n'avait que le SELECT via `…0003`).

**Édition / suppression d'un aidant** (`profiles` + `auth.users`) : passe par 2 Edge Functions service-role (`requireAdmin` vérifie le JWT appelant + le rôle), **pas** par la RLS :
- `admin-update-caregiver` — propage le changement d'email à `auth.users.email` (`email_confirm:true`) en plus de `profiles`. Le `role` n'est jamais modifié (lecture seule côté UI).
- `admin-delete-caregiver` — **refuse en 409** si l'aidant a ≥1 bénéficiaire (garde-fou anti-orphelins ; la FK `caregiver_id` est en CASCADE donc sans ce garde-fou la suppression effacerait silencieusement les bénéficiaires), refuse aussi l'auto-suppression, sinon `auth.admin.deleteUser` (cascade → `profiles`).

**Pour rendre un compte admin** : `UPDATE profiles SET role='admin' WHERE email='...'` (CHECK élargi à `caregiver | beneficiary | admin`).

**Action « Relancer »** sur `/admin/appels` : INSERT direct via client supabase (RLS admin autorise) + `supabase.functions.invoke('initiate-call', { body: { call_id } })`. Pas d'Edge Fn dédiée.

**Différé** (non inclus dans ce lot) : impersonate / vue « comme cet aidant », page emails séparée (l'info est déjà sur `calls.report_email_sent_at`).

### Observabilité & robustesse (Lot 4)

- **Table `system_events`** (cf. tables principales) écrite via `_shared/systemEvents.ts` (Edge) ou `persistence/system-events.js` (voice-bridge). Best-effort, jamais bloquant. Lue par la section « Événements système » de `/admin/sante`.
- **Twilio statusCallback** : `POST /scheduled-status` sur le voice-bridge reçoit les transitions Twilio (`no-answer`, `busy`, `failed`, `canceled`, `completed`) et marque le call AICOUTE via `markCallByTwilioStatus` (`missed`/`failed`). Permet de détecter un no-answer en quelques secondes au lieu d'attendre 120 s côté passe B. ⚠️ Ce webhook **marque seulement le statut** ; la relance + l'email no-answer sont décidés par la **passe B2** de `schedule-calls` (sur les `missed`/`failed` non traités), pas ici. Sur chaque statut terminal, lance aussi `captureTwilioCost(sid)` (fire-and-forget) : le champ `price` d'un appel Twilio est renseigné de façon **asynchrone** (null au moment du `completed`), donc on poll l'API Twilio plusieurs fois avec délai croissant jusqu'à l'obtenir, on convertit en EUR (USD→EUR si besoin) et on écrit `calls.twilio_cost_eur`. Best-effort : si échec, l'UI garde l'estimation par la durée. Pour les appels **antérieurs** à cette capture, endpoint manuel `POST /backfill-twilio-costs` (auth `MODECT_INTERNAL_TOKEN`, body `{limit}`) qui parcourt les `completed` sans `twilio_cost_eur` et récupère leur prix (déjà finalisé → fetch unique sans polling). **Appels ENTRANTS (Canal 3)** : ils n'ont PAS de `statusCallback` (l'appel n'est pas créé par nous) → le coût Twilio est capturé par un autre chemin : le webhook `/inbound-voice` passe le `CallSid` en `<Parameter name="twilio_sid">`, et `handleAicouteCallConnection` lance `captureTwilioCost(sid)` à la fermeture de l'appel. Le coût IA, lui, est écrit par `recordCallTokens` (commun sortant/entrant). Les deux alimentent la « Vue d'ensemble » `/admin` (somme sans filtre d'origine).
- **Vue coûts IA** sur `/admin` : barchart 30 jours + total mensuel, calculé sur `calls.ai_cost_eur_real` (snapshot écrit par le voice-bridge en fin d'appel).
- **Test tronqué** : `npm run test:email-report` (cf. [scripts/test-email-report.mjs](scripts/test-email-report.mjs)) valide la chaîne `save-transcript → generate-summary → email Resend` sans Twilio ni OpenAI Realtime. 30 s, 0 €.
- **Checklist pré-release** : [docs/RELEASE_CHECKLIST.md](docs/RELEASE_CHECKLIST.md) pour les vérifications manuelles qu'aucun test automatique ne couvre (vrai appel Twilio avec audio).

### Compte-rendu partageable (lien public 48h)
L'email post-appel (envoyé par [generate-summary](supabase/functions/generate-summary/index.ts), renvoyé par [resend-report](supabase/functions/resend-report/index.ts)) ne pointe plus vers `/historique/:id` (back-office, login requis) mais vers une **page publique sans login** `/r/:token` ([PublicReport.tsx](apps/web/src/pages/public/PublicReport.tsx), route hors `AuthGuard`, servie sur les deux hôtes). But : partage avec des proches sans compte.
- **Jeton** : `issueReportToken` ([_shared/reportToken.ts](supabase/functions/_shared/reportToken.ts)) écrit `calls.report_token` (aléatoire 32o) + `report_token_expires_at = now+48h`, ré-émis à **chaque** envoi/renvoi (l'ancien lien devient invalide). Base URL surchargeable via env `PUBLIC_REPORT_URL` (défaut `https://www.aicoute.fr`).
- **Données** : la page fetch [get-report](supabase/functions/get-report/index.ts) (`verify_jwt=false`, service-role) → lookup par token + check expiration (404 inconnu / 410 expiré). `calls` n'est PAS ouvert à anon : le token EST le secret, et get-report ne renvoie qu'un sous-ensemble « rapport » (jamais email aidant, coûts, sid Twilio).
- **Destinataires** : l'email part à l'aidant **+** `beneficiaries.report_recipients` (proches), assemblés/dédoublonnés/validés par `normalizeRecipients` (`_shared/email.ts`). Tous en `To:` (un seul email). Gouverné par l'opt-in `notify_call_report`.

### Redirections legacy (dans [App.tsx](apps/web/src/App.tsx))
`/planning → /contexte?tab=planning` (page Planning supprimée → onglet), `/sessions → /contexte?tab=planning`, `/reports → /historique`, `/reports/:id → /historique/:id`, `/settings → /compte`, `/beneficiary → /contexte`, `/beneficiary/:id → /contexte` (avec sélection auto), `/memories → /dashboard`, `/setup → /compte`, `/veille → /signaux` (page Veille remplacée par Signaux).

### Worker planning + politique no-answer

**Pré-création des calls** : les calls correspondant à chaque créneau récurrent sont pré-créés à l'avance sur un horizon de **15 jours** par [regenerate-future-calls](supabase/functions/regenerate-future-calls/index.ts). Idempotent grâce au UNIQUE constraint `calls(schedule_id, scheduled_at)` (non-partial — cf. Bugs connus). **Auto-correctif** : pour un schedule actif, la fonction **élague d'abord** les `calls` `status='scheduled'` futurs de ce schedule dont le `scheduled_at` n'est plus dans la projection courante (créneaux orphelins après un changement d'heure/de jours), **puis** upsert les bons créneaux. Elle ne touche jamais aux statuts `notified`/`in_progress`/`completed` → un appel `completed` posé sur un créneau futur (appel déclenché en avance) **bloque** définitivement la recréation `scheduled` de ce créneau (slot consommé ; le rouvrir = supprimer la ligne à la main). Déclenché :
- Par le trigger SQL `session_schedules_regenerate_calls` à chaque INSERT/UPDATE/DELETE sur `session_schedules` (via pg_net, secrets lus via `vault.decrypted_secrets`)
- Côté client back-office après save dans [useSessionSchedule.ts](apps/web/src/hooks/useSessionSchedule.ts) (belt+suspenders)
- Devrait l'être par un cron quotidien pour étendre l'horizon (TODO : à câbler avec pg_cron une fois validé)

Conséquences :
- `calls.scheduled_at` est désormais **immutable après création** (= créneau prévu original)
- `calls.notified_at` est l'heure **effective** de déclenchement (écrit par `initiate-call`)
- Le bouton « Déclencher maintenant » du back-office admin n'écrit donc plus que `notified_at`, laissant `scheduled_at` intact pour la traçabilité
- Stats SQL faciles : `SELECT COUNT(*) WHERE scheduled_at BETWEEN X AND Y`

[schedule-calls](supabase/functions/schedule-calls/index.ts) tourne via pg_cron toutes les minutes et exécute 3 passes :
- **A — Déclenchement principal** : lit `calls` en `scheduled` (attempt_number=1) dont `scheduled_at` tombe dans ±90s → déclenche `initiate-call`. **Ne crée plus** de calls (c'est `regenerate-future-calls` qui le fait à l'avance).
- **B — Politique no-answer (relances + email final)** — **découplée du statut `notified`** (migration `20260629000001`, colonne `calls.no_answer_handled_at`). Historiquement la passe B ne regardait que les `notified` → mais le statusCallback Twilio (et l'AMD) passe l'appel en `missed` en ~30s, AVANT le timeout B → la passe B ne le voyait jamais et **aucune relance n'était créée** (bug corrigé 2026-06-29). Désormais 2 sous-étapes :
  - **B1 — filet « webhook perdu »** : un appel resté `notified` au-delà de `no_answer_timeout_seconds` → passe `missed` (sans rien décider de plus).
  - **B2 — arbitrage** : scanne les appels **`missed` OU `failed`** d'un planning (`schedule_id` non NULL → exclut entrants/campagnes) **non encore traités** (`no_answer_handled_at IS NULL`), peu importe QUI les a marqués (webhook, AMD, ou B1). En **claim-then-act** (pose `no_answer_handled_at` d'abord, condition `IS NULL`, anti double-traitement entre ticks) : si `attempt_number ≤ retry_count` → crée la relance (`attempt+1`, `scheduled_at = now + retry_interval_minutes`) ; sinon → email **« ⚠️ XXX n'a pas répondu »** (`noAnswerEmailHtml`, si `notify_on_no_answer`) à **l'aidant + `report_recipients`** (via `normalizeRecipients`). Garde-fou anti-acharnement : appel non abouti vieux de >6h ignoré. La migration **backfill** tous les appels terminaux existants (`no_answer_handled_at` posé) pour ne pas rejouer le passé. L'heure dans l'email est formatée avec `timeZone` = `session_schedules.timezone` (défaut `Europe/Paris`) — l'Edge tourne en UTC (cf. Bugs connus).
- **C — Déclenchement des retries** : `calls` en `scheduled` avec `attempt_number > 1` et `scheduled_at ≤ now` → trigger `initiate-call`.

[initiate-call](supabase/functions/initiate-call/index.ts) déclenche l'appel Twilio via le voice-bridge puis écrit `notified_at = now()` + `twilio_call_sid` au passage en `notified` (origine du timer no-answer). Cf. **Canal 2 — Appels planifiés Twilio** ci-dessus pour le flow complet.

### Signaux faibles structurés
[generate-summary](supabase/functions/generate-summary/index.ts) produit `alerts: Array<{category, severity, evidence}>`. Catégories : `health` (douleur, sommeil, médication, fatigue physique) · `mood` (tristesse, anxiété, lassitude) · `cognition` (oublis, confusion, mots qui manquent) · `social` (solitude, isolement, conflit familial) · `autonomy` (chute, alimentation, gestes du quotidien) · `other`. Sévérité : `low` / `medium` / `high`. Rendu côté UI dans CallDetail (cartes avec icône + badges) et dans l'email aidant. Ces signaux sont en plus agrégés sur **deux pages dédiées** (cf. routes ci-dessus) : **`/admin/signaux`** (admin, toutes sévérités, boîte de réception + suivi des actions via `signal_actions`) et **`/signaux`** (aidant, lecture seule, signaux du bénéficiaire sélectionné).

### Largeurs visuelles (charte)
- **Toutes les pages back-office** (aidant + admin, y compris le wizard) : conteneur `max-w-[1400px] mx-auto px-4 py-8` (uniformisé 2026-06-09).
- À l'intérieur, certaines colonnes de contenu gardent une largeur de lecture (`max-w-3xl` pour Mémoire/Planning, grille 3 colonnes pour Profil/Compagnon IA).

## Dashboard ORGANISATION — campagnes d'appels en masse (2026-06-19)

**Deuxième back-office**, distinct du parcours aidant « particulier », pour les comptes **personne morale** (EHPAD, mairies, assureurs…). **Décision clé : on branche sur `profiles.account_type='organization'`** (colonne qui existait déjà, posée à l'inscription ; `role` reste `caregiver`) — PAS de nouveau rôle. Cœur du dashboard = les **campagnes** : appeler en masse une liste de bénéficiaires, **une fois chacun** (avec relances si pas de réponse), pendant une fenêtre de dates + une plage horaire quotidienne, à **concurrence bornée PAR campagne**. **Facturation différée / hors chantier** : les appels de campagne ne décomptent PAS de minutes pour l'instant (orgs au contrat) ; la durée reste stockée (`calls.duration_seconds`).

### Routage & layout
- [useAccountKind.ts](apps/web/src/hooks/useAccountKind.ts) : `isAdmin` / `isOrganization` (= account_type org ET pas admin). `AppHostRoot` ([App.tsx](apps/web/src/App.tsx)) dispatch : admin → `/admin`, org → `/org/campagnes`, sinon `/dashboard`. Gardes : [OrgOnly.tsx](apps/web/src/components/OrgOnly.tsx) (réservé org) ; [AidantOnly.tsx](apps/web/src/components/AidantOnly.tsx) redirige aussi l'org vers `/org`.
- [OrgLayout.tsx](apps/web/src/pages/org/OrgLayout.tsx) : sidebar propre (Bénéficiaires · Campagnes · Appels réalisés · Signaux + Mon compte), sans AppHeader ni bénéficiaire « sélectionné » global. `/org/compte` réutilise [ComptePage](apps/web/src/pages/compte/ComptePage.tsx) sous ce layout.

### Modèle de données (migrations `20260619000001`→`000006`)
- `beneficiaries.comment` (TEXT) : commentaire libre, colonne affichée + filtrable côté org. Les bénéficiaires d'org sont **« légers »** (identité + téléphone + commentaire ; **PAS** de personnalité/mémoire/voix/appels entrants propres — c'est la CAMPAGNE qui porte prompt/persona/langue/durée). Créés avec **`preferred_engine='gemini'` forcé** par [useOrgBeneficiaries.ts](apps/web/src/hooks/useOrgBeneficiaries.ts) (le défaut SQL est `openai`).
- `campaigns` : `org_id` (FK profiles), `title`, `comment`, `starts_on`/`ends_on` (DATE), `prompt_id` (FK prompts, modèle de base), **`custom_prompt`** (prompt ÉDITÉ propre à la campagne — TEMPLATE à variables, prime sur prompt_id), `language`, `ai_persona_name` (= `{{persona}}`), `daily_start_time`/`daily_end_time`/`timezone` (plage horaire d'émission), `max_concurrent_calls` (cœur du moteur), `max_call_minutes` (coupure dure), `retry_count`/`retry_interval_minutes` (relances éditables), `status` (`draft|running|paused|completed`). RLS org (`org_id=auth.uid()`) + admin SELECT.
- `campaign_beneficiaries` (jointure campagne↔bénéficiaire) ; `campaign_activity_periods` (un segment par GO→PAUSE : `started_at`/`ended_at`).
- `calls.campaign_id` (FK campaigns, `ON DELETE SET NULL`) + valeur `origin='campaign'` (CHECK étendu). Appels de campagne = SORTANTS, sans `schedule_id`.
- GRANTs explicites `authenticated` + `service_role` (tables créées en migration brute, cf. piège).

### Moteur (cœur) — [campaign-dispatch](supabase/functions/campaign-dispatch/index.ts)
Cron **toutes les minutes** (migration `20260619000003` : fonction SECURITY DEFINER `dispatch_campaigns()` qui lit les secrets dans **Vault** — même pattern que la régénération, car `app.settings.*` est cassé sur Supabase managé). Pour chaque campagne `running` : (1) fenêtre de dates (timezone) → passé `ends_on` = `completed` ; (2) **libère les appels zombies** (`notified` trop vieux = statusCallback Twilio perdu → `missed` ; `scheduled` jamais déclenché → `failed`) ; (3) **auto-complétion** si tous les bénéficiaires sont joints/épuisés/injoignables et rien en vol ; (4) plage horaire quotidienne ; (5) **concurrence PAR campagne** = `max_concurrent_calls − (en vol : scheduled+notified+in_progress)` → sélectionne les éligibles (jamais tentés en priorité, sinon relance due selon `retry_count`/`retry_interval`) → crée un `call` (`origin='campaign'`) + invoke `initiate-call`. **⚠️ Garantie anti-doublon : un bénéficiaire avec ≥1 appel de campagne `completed` est classé `done` et exclu définitivement** (les relances ne concernent que `missed`/`failed`). Réutilise toute la plomberie du **Canal 2** (Twilio `/scheduled-call`, statusCallback no-answer, flush → save-transcript → generate-summary).
- [initiate-call](supabase/functions/initiate-call/index.ts) : durée max = `campaign.max_call_minutes` (sinon planning, sinon filet) ; **engine = `gemini` IMPOSÉ si `call.campaign_id`** (Gemini-only ; le défaut SQL `preferred_engine='openai'` ne s'applique donc pas aux campagnes).
- [callContext.ts](supabase/functions/_shared/callContext.ts) branche campagne : TEMPLATE = `campaign.custom_prompt` → `prompt_id.outbound_body` → défaut de la langue ; `{{persona}}`=`ai_persona_name`, `{{langue}}`=`campaign.language`, durée cible = `max_call_minutes`. ⚠️ `campaign.custom_prompt` reste un **TEMPLATE** (variables résolues PAR bénéficiaire à l'appel) — ≠ `beneficiaries.custom_prompt` (concret, utilisé tel quel).
- [generate-summary](supabase/functions/generate-summary/index.ts) : **PAS d'email par appel** pour `origin='campaign'` (éviterait des centaines d'emails sur une boîte unique) ; le résumé + les alertes sont quand même calculés/stockés → remontés dans `/org/signaux`.

### Pages org
- `/org/beneficiaires` ([OrgBeneficiaires.tsx](apps/web/src/pages/org/OrgBeneficiaires.tsx)) : table triable/filtrable (nom/prénom/tél/commentaire) + CRUD ([BeneficiaryFormModal](apps/web/src/pages/org/BeneficiaryFormModal.tsx)) + **import CSV** ([ImportCsvModal](apps/web/src/pages/org/ImportCsvModal.tsx) + [csv.ts](apps/web/src/pages/org/csv.ts), modèle de démo téléchargeable, normalisation E.164) + bouton **Historique** par bénéficiaire ([BeneficiaryHistoryModal](apps/web/src/pages/org/BeneficiaryHistoryModal.tsx) : transcript/qualité/.wav).
- `/org/campagnes` ([OrgCampagnes.tsx](apps/web/src/pages/org/OrgCampagnes.tsx), [useCampaigns](apps/web/src/hooks/useCampaigns.ts)) : liste + compteurs (bénéf. / à date / à faire / établies, auto-refresh si une campagne tourne). `/org/campagnes/:id` ([OrgCampagneDetail](apps/web/src/pages/org/OrgCampagneDetail.tsx), [useCampaign](apps/web/src/hooks/useCampaign.ts)) : 3 onglets —
  - **Administratif** ([CampaignAdminTab](apps/web/src/pages/org/tabs/CampaignAdminTab.tsx)) : config + **éditeur de prompt** (sélecteur de modèle bibliothèque + zone éditable `custom_prompt` + « Charger le modèle ») + relances éditables + **zone danger** (effacer).
  - **Bénéficiaires** ([CampaignMembersTab](apps/web/src/pages/org/tabs/CampaignMembersTab.tsx)) : ajout/retrait (multi-sélection) + bouton **« Appeler »** (appel immédiat hors plage/GO, via `callNow`) + colonne **« Appel abouti »** (date si `completed`).
  - **Activité** ([CampaignActivityTab](apps/web/src/pages/org/tabs/CampaignActivityTab.tsx)) : **KPI** (appels passés / temps passé / à passer) + interrupteur **GO/PAUSE** (ouvre/ferme une période, écrit `status`) + **journal d'événements** (GO/PAUSE + cycle de vie des appels, « en direct ») + tableau des périodes. Auto-refresh 20 s ; statut honnête (en cours / hors plage / avant début) via [campaignWindow.ts](apps/web/src/pages/org/campaignWindow.ts).
- `/org/appels` ([OrgAppels.tsx](apps/web/src/pages/org/OrgAppels.tsx)) : appels réalisés (date · bénéficiaire · **campagne** · statut · durée + Détail/Qualité/.wav).
- `/org/signaux` ([OrgSignaux.tsx](apps/web/src/pages/org/OrgSignaux.tsx)) : reprise de l'écran admin scopée org, avec **journal de suivi** `signal_actions` (RLS étendue) + filtres statut/sévérité + **filtre par campagne** (déroulant à droite).
- Vues/primitives partagées : [campaignViews.tsx](apps/web/src/pages/org/campaignViews.tsx) (KPI/journal/périodes, réutilisés org + admin), [Modal](apps/web/src/pages/org/Modal.tsx), [callStatus](apps/web/src/pages/org/callStatus.tsx), [campaignStatus](apps/web/src/pages/org/campaignStatus.tsx), [alerts](apps/web/src/pages/org/alerts.tsx), [CallTranscriptModal](apps/web/src/pages/org/CallTranscriptModal.tsx).

### Page admin transverse
`/admin/campagnes` ([AdminCampagnes.tsx](apps/web/src/pages/admin/AdminCampagnes.tsx)) : **toutes** les campagnes (RLS admin) + colonne **Organisation**, placée dans la sidebar admin **entre Bénéficiaires et Appels**. `/admin/campagnes/:id` ([AdminCampagneDetail.tsx](apps/web/src/pages/admin/AdminCampagneDetail.tsx)) : fiche **LECTURE SEULE** (config + prompt effectif + bénéficiaires + KPI/journal/périodes, aucun bouton d'action).

### RLS ajoutées (au-delà des tables campagnes)
- storage `caregiver_read_own_recordings` (`20260619000001`) : l'aidant/organisation écoute les `.wav` de SES propres appels (jusque-là admin-only) → bouton .wav côté org.
- `signal_actions` `caregiver_select/insert_signal_actions` (`20260619000004`) : l'org (et l'aidant) lit + journalise le suivi des signaux de SES appels (`author_id` forcé à `auth.uid()`, modèle append-only inchangé).

### Déploiement de ce chantier
`supabase db push` (migrations `20260619000001`→`000006`) + `supabase functions deploy campaign-dispatch initiate-call get-call-context generate-summary realtime-token`. Le cron `aicoute-campaign-dispatch` est posé par la migration et s'appuie sur les secrets Vault déjà présents. Aucune modif voice-bridge (les campagnes réutilisent `/scheduled-call`).

## Achat de minutes (Stripe) — modèle « bon d'achat / code »

Paiement réel des **packs de minutes** (`MINUTE_PACKS`) via **Stripe Checkout (paiement unique)**, branché derrière la section TARIFS de la home ET le back-office. Distinct du modèle `subscriptions`/`PLAN_TIERS` (appels/semaine) et de l'**abonnement « Le contrôle »** (cf. section dédiée) qui coexistent. **État (2026-07-02) : déployé en prod ET ACTIF** — `STRIPE_SECRET_KEY` est posé et fonctionnel (les paiements marchent). ⚠️ La clé est une **clé RESTREINTE `rk_live_`** (compte partagé, `acct_1Td6Pg…`) : ses **permissions** doivent couvrir tout ce que le code appelle — **Checkout Sessions: Write** (packs + abonnement) **ET Billing Portal Sessions: Write** (portail factures de « Le contrôle »). Les permissions d'une clé restreinte n'étant pas toujours éditables après coup, ajouter un scope = souvent **régénérer une nouvelle clé** (type « Powering an integration you built ») puis `supabase secrets set STRIPE_SECRET_KEY=…` + redeploy des fonctions Stripe. `STRIPE_WEBHOOK_SECRET` (whsec_) est indépendant de la clé API.

⚠️ **Compte Stripe PARTAGÉ avec une autre app** → le webhook ne traite QUE les paiements portant `metadata.app === 'aicoute'` (sinon 200 sans rien faire).

**Flux** :
1. Vitrine `#tarifs` ([Pricing.tsx](apps/web/src/marketing/components/Pricing.tsx)) ou `/compte` → `startCheckout(packId)` ([lib/checkout.ts](apps/web/src/lib/checkout.ts)) → `create-checkout-session` → redirection Stripe.
2. **Invité** (non connecté) → success `/achat/merci?session_id=…` ([PurchaseSuccess.tsx](apps/web/src/pages/public/PurchaseSuccess.tsx), route publique) ; le webhook génère un **CODE**, l'insère dans `minute_codes`, l'envoie par email (`purchaseCodeEmailHtml`) ; la page **poll** `get-purchase-code` et l'affiche. L'aidant saisit ensuite le code dans `/compte → Mes achats` (« J'ai un code » → `redeem-code` → crédite `minute_purchases`).
3. **Connecté** → `create-checkout-session` met `caregiver_id` dans les metadata → le webhook **crédite directement** `minute_purchases` (pas de code) ; retour `/compte?achat=ok` (le crédit est asynchrone → la page rafraîchit le solde quelques secondes après).

**Edge Functions** (toutes `verify_jwt=false`, auth interne) : `create-checkout-session` (publique, JWT optionnel), `stripe-webhook` (signature via `constructEventAsync` + SubtleCryptoProvider, garde `metadata.app`, idempotence `stripe_session_id`), `get-purchase-code` (poll page succès), `redeem-code` (auth JWT, crédit atomique), `admin-credit-minutes` (requireAdmin → `minute_adjustments`). Helper partagé [_shared/stripe.ts](supabase/functions/_shared/stripe.ts) : `getStripe()` (import `https://esm.sh/stripe@…?target=deno`), catalogue SERVEUR `STRIPE_PACKS` (source de vérité minutes/montants — jamais le client), `generateCode()`. Price IDs en env `STRIPE_PRICE_RENDEZVOUS|LIEN|PRESENCE`.

**Sécurité** : prix/quantité toujours côté serveur ; `minute_codes` jamais exposée au client ; INSERT crédits retiré à `authenticated` (écriture service-role only) ; **GRANT explicite `service_role`** sur les 3 tables (cf. Bugs connus — sans ça l'INSERT serveur échoue en « permission denied »).

## Abonnement « Le contrôle » (Stripe RÉCURRENT — parcours paiement-d'abord) (2026-07-02)

4ᵉ offre de la section TARIFS vitrine ([Pricing.tsx](apps/web/src/marketing/components/Pricing.tsx), carte `ControlCard` **en 1ʳᵉ position**, grille resserrée à 4 blocs) : **abonnement mensuel 18 €/mois** = 1 appel de contrôle par jour + alerte email aux proches si non-réponse. **≠ packs de minutes** (récurrent, `mode:'subscription'`). **DÉPLOYÉ ET VALIDÉ EN PROD** (testé de bout en bout avec un vrai paiement live).

**Principe « paiement d'abord, compte ensuite »** : au paiement le compte n'existe pas → l'abonnement Stripe est **retenu** puis **rattaché** au compte à l'inscription (même idée que les codes invités des packs, mais pour un abonnement).

**Tables :**
- `subscriptions` — palier `plan_tier='controle'` ajouté au CHECK (migration `20260702000001`) + colonnes `stripe_customer_id`/`stripe_subscription_id`. ⚠️ Cette table n'était grantée qu'à `authenticated` (l'essai gratuit s'insère côté client) → GRANT `service_role` ajouté par `20260702000002` (cf. Bugs connus : sans lui le claim échouait, laissant un compte abonné SANS ligne subscriptions).
- `pending_control_subscriptions` (`20260702000001`) — abonnement payé AVANT le compte : `stripe_session_id` UNIQUE (idempotence webhook + clé de rattachement), `stripe_customer_id`/`stripe_subscription_id`, `buyer_email`, `amount_eur`, `status` pending|claimed|canceled, `caregiver_id`, `claimed_at`. **Jamais exposée au client** (RLS admin SELECT, écriture service-role) ; index partiel sur `lower(buyer_email)` pour le rattachement de secours par email.

**Flux :** clic « Souscrire » → `startControlCheckout()` ([lib/checkout.ts](apps/web/src/lib/checkout.ts)) → `create-checkout-session` (branche `body.plan==='controle'` → `mode:'subscription'`, `success_url=/auth/register?sub={CHECKOUT_SESSION_ID}`) → Stripe collecte paiement + email → **retour vers l'inscription pré-remplie**. `stripe-webhook` (`meta.plan==='controle'`) insère la ligne `pending_control_subscriptions`. À l'inscription, [Register.tsx](apps/web/src/pages/auth/Register.tsx) lit `?sub=`, pré-remplit l'email (Edge `get-control-checkout`) et **mémorise le session_id en localStorage** (`aicoute.pending_control_sub`, doit survivre à la confirmation d'email). Au retour authentifié, `AppLayout`/`OrgLayout` appellent `claimPendingControl()` ([lib/controlSubscription.ts](apps/web/src/lib/controlSubscription.ts)) → Edge `claim-control-subscription` : rattache l'attente (par session_id sinon email), claim ATOMIQUE (claimed→pending en **rollback** si la pose échoue → auto-réparation), pose la ligne `subscriptions` (`controle`, active, 7 appels/sem). **Le wizard attend `claimPendingControl()` avant** de lire l'abonnement et d'**auto-configurer un planning quotidien 7j/7** (avec `notify_on_no_answer`) ([BeneficiaryWizard.tsx](apps/web/src/pages/beneficiary/BeneficiaryWizard.tsx)) ; l'onglet Planning ([CaregiverPlanningTab.tsx](apps/web/src/pages/contexte/tabs/CaregiverPlanningTab.tsx)) tente aussi le rattachement avant de montrer l'écran essai (sinon un abonné verrait à tort la proposition d'essai — la bascule essai↔planning dépend de `subscription` présent).

**Espace compte abonné** ([ComptePage.tsx](apps/web/src/pages/compte/ComptePage.tsx)) : pour un abonné `controle`, les blocs minutes n'ont pas de sens → **masqués** (« Acheter des minutes », « J'ai un code », carte « Minutes disponibles », historique de packs vide). Carte **« Mon abonnement »** (Le contrôle · 18€/mois · Actif) avec bouton **« Gérer mon abonnement · factures »** → Edge `create-billing-portal-session` (résout le customer Stripe de l'abonné, backfill depuis la subscription si `stripe_customer_id` manquant, puis `billingPortal.sessions.create`) → **portail client Stripe** (factures PDF, moyen de paiement, résiliation). ⚠️ Le portail exige la permission **Billing Portal Sessions: Write** sur la clé restreinte **ET** l'activation du Customer Portal dans le dashboard Stripe (Settings → Billing).

**Edge Functions** (toutes `verify_jwt=false`) : `create-checkout-session` (branche abonnement), `stripe-webhook` (branche rétention), `get-control-checkout` (prefill email par session_id), `claim-control-subscription` (rattachement + pose subscriptions), `create-billing-portal-session` (portail factures/gestion). Catalogue serveur `CONTROL_PLAN` + `controlPriceId()` dans [_shared/stripe.ts](supabase/functions/_shared/stripe.ts) ; Price ID en env `STRIPE_PRICE_CONTROLE`. Type partagé : `PlanTier` + `'controle'` + `PLAN_TIERS.controle`.

## Démo vitrine (www.aicoute.fr)
Deux modes (navigateur / téléphone), accessibles depuis la home (section `#essai`, `apps/web/src/marketing/components/Demo.tsx`). **Moteur figé sur Gemini** (le toggle `EngineToggle` a été retiré — décision produit) : `engine='gemini'` est propagé aux deux modals et persisté dans `demo_calls.engine`. Reste au centre un sélecteur de **langue** ; le mode téléphone a en plus un **sélecteur d'indicatif pays** (France par défaut, reconstruction E.164 à l'envoi). _La plomberie démo multi-moteur (bridges OpenAI + Gemini, web + téléphone) reste en place et OpenAI est testable par téléphone via `/admin/sante`._

L'architecture est **asymétrique** côté web entre les deux moteurs :
- OpenAI web → WebRTC direct navigateur ↔ OpenAI (token éphémère via Edge Fn)
- Gemini web → WebSocket navigateur ↔ voice-bridge ↔ Gemini (proxy serveur, car Gemini n'a pas d'ephemeral token public et la clé Google doit rester serveur)

**Mode 1 — Navigateur (OpenAI)** : `DemoWebModal.tsx` avec `engine='openai'`. Ephemeral token via Edge Fn `public-realtime-token` (rate-limit IP 5/h). Utilise `RealtimeSession` de `@modect/shared`. WebRTC direct vers OpenAI.

**Mode 1bis — Navigateur (Gemini)** : `DemoWebModal.tsx` avec `engine='gemini'`. Ouvre une WS vers `${VITE_VOICE_BRIDGE_URL}/ws/gemini-web` (proxy voice-bridge). Utilise `GeminiLiveSession` de `@modect/shared` qui charge l'AudioWorklet `/gemini-audio-worklet.js` (capture mic → PCM16 16 kHz) et joue les chunks PCM16 24 kHz reçus via un `AudioContext`. Côté serveur : `services/voice-bridge/src/engines/gemini-bridge-web.js` proxy vers Gemini avec vérif d'origine (`ALLOWED_ORIGINS`) + rate-limit IP 5/h (`LIMITS.perIpWeb`).

**Mode 2 — Téléphone (OpenAI ou Gemini)** : `DemoPhoneModal.tsx`. POST le numéro + `engine` vers `${VITE_VOICE_BRIDGE_URL}/call`. Le service crée un appel Twilio, sert un TwiML qui ouvre une WS `/media-stream` ; le `<Parameter name="engine">` propage le choix. Au start de la WS, server.js dispatche vers `engines/openai-bridge.js` (µ-law direct) ou `engines/gemini-bridge.js` (conversion µ-law 8 kHz ↔ PCM16 16/24 kHz via `engines/audio.js`). Rate-limit IP 3/h + numéro 3/24h. Coupure serveur 2 min (`MAX_CALL_SECONDS=120`).

Numéro Twilio prod : `+33 9 39 03 52 69`. Modèle Gemini par défaut surchargeable via env `GEMINI_MODEL` / `GEMINI_VOICE`.

**Tracking des démos** : chaque démo (web ou téléphone, OpenAI ou Gemini) crée une row dans `demo_calls`. Champs : mode, engine, started_at, ended_at, duration_seconds, phone_prefix (6 chars, mode téléphone uniquement), twilio_cost_eur (estim. durée), openai_cost_eur (estim. durée — nom historique, applicable aux deux moteurs), openai_cost_eur_real (coût IA réel par tokens — applicable aux deux moteurs, dispatch tarifs via `computeAiCostEur(engine, tokens)`), tokens_input_audio + tokens_input_audio_cached + tokens_output_audio + tokens_input_text + tokens_output_text (mutualisés ; `input_audio_cached` reste à 0 pour Gemini qui ne facture pas le cache audio). Côté web : `DemoWebModal` appelle l'Edge Function `log-demo` (actions `start`/`end`) → écrit ended_at + duration + estimation par durée. **Coût IA réel (tokens)** : pour le web **Gemini**, les tokens ne sont visibles que côté serveur (proxy `gemini-bridge-web.js`), donc le client propage le `demoId` en query (`/ws/gemini-web?demoId=…`) et le voice-bridge complète `openai_cost_eur_real` + `tokens_*` via `recordDemoRealCost` (colonnes disjointes de log-demo → pas de course). Le web **OpenAI** (WebRTC direct) n'a pas encore ce câblage → seule l'estimation par durée est remontée. Côté téléphone : `services/voice-bridge/src/tracking.js` écrit directement dans Supabase via service role, `demoCallId` propagé via TwiML `<Parameter>`, coût réel inclus. Tarifs : OpenAI ($32/$64 in/out par M tokens) et Gemini ($3/$12) hardcodés dans tracking.js, conversion USD→EUR à 0,92. **Consultable dans `/admin/appels` onglet « Démos vitrine »** ([AdminDemosTab.tsx](apps/web/src/pages/admin/AdminDemosTab.tsx), lecture directe `demo_calls` via policy `admin_all_demo_calls`). _Legacy : l'ancienne page publique `/track_calls` (clé `DEMO_TRACK_KEY` + Edge Function `list-demos`) a été retirée le 2026-06-07 ; la fonction `list-demos` et le secret restent déployés mais ne sont plus utilisés._

## Vitrine — pages de contenu, SEO & prerendering

### Pages publiques hors home (vitrine)
Routes déclarées dans [App.tsx](apps/web/src/App.tsx) hors `AuthGuard`, servies sur les deux hôtes (utiles sur la vitrine) :
- `/a-propos` — [About.tsx](apps/web/src/marketing/About.tsx) (récit « Notre histoire »)
- `/etablissements` — [Etablissements.tsx](apps/web/src/marketing/Etablissements.tsx) (page B2B EHPAD/résidences ; `Header`+`Footer` complets, SEO `META`+useEffect, formulaire contact B2B)
- `/mentions-legales`, `/cgu`, `/cgv`, `/rgpd`, `/ia-act` — pages légales dans [src/marketing/legal/](apps/web/src/marketing/legal/). **CGV** (`/cgv`, [CGV.tsx](apps/web/src/marketing/legal/CGV.tsx)) = vente B2C des packs de minutes (Stripe), trame **à relire juridiquement** : service exécuté immédiatement + renonciation au droit de rétractation, minutes sans expiration, clause médiateur conso **à compléter** (pas encore désigné). Lien footer **« CGV » ouvre dans un nouvel onglet** (`target=_blank`), regroupé avec CGU sur une ligne (« CGU — CGV »).
- `/charte-ethique` — [CharteEthique.tsx](apps/web/src/marketing/legal/CharteEthique.tsx) (8 engagements éthiques, même `LegalLayout`). Lien dans le Footer (bloc Entreprise) + CTA « Lire notre charte éthique complète » de la section Sécurité de la home.
- `/conseils` + 4 guides — **section éditoriale** (cf. sous-section dédiée ci-dessous).

Les pages **légales + À propos** réutilisent **[LegalLayout.tsx](apps/web/src/marketing/legal/LegalLayout.tsx)** : barre minimale (logo + « ← Retour à l'accueil »), titre Fraunces, date de MAJ optionnelle, `Footer`, + primitives typo exportées (`Section`/`P`/`UL`/`LI`/`Mail`). Liens internes en `<a>` simple (aucun hook react-router) → composants **SSR-safe**. `document.title` posé via `useEffect`. Contenu légal **adapté** de corraict.com (même société Oaventure EURL) au modèle B2C d'Aicoute (responsable de traitement, sous-traitants OpenAI/Google/Twilio/Resend/Supabase/Render/Netlify, volet données de santé art. 9 + consentement) ; CGU/CGV = trames **génériques à relire juridiquement**. `/etablissements` et `/conseils`, eux, utilisent le `Header`+`Footer` **complets** (pas `LegalLayout`). Aucune de ces pages n'est prerendue (rendu client, SEO secondaire — cf. JSON-LD).

### Section éditoriale « Conseils » (`/conseils` + 4 guides)
Hub SEO/acquisition + 4 guides aidants, sous [src/marketing/conseils/](apps/web/src/marketing/conseils/). Routes : `/conseils` (hub) + `/conseils/{rompre-isolement-personne-agee,parent-age-seul-vivre-loin,aider-parent-veuf-isolement,culpabilite-ne-pas-appeler-parents}`. **Système de design propre** dans [ConseilsLayout.tsx](apps/web/src/marketing/conseils/ConseilsLayout.tsx) (≠ LegalLayout) : `Header`+`Footer` marketing + coquille d'article `GuidePage` (fil d'Ariane, `GuideHero`, prose `Section`/`P`/`UL`/`LI`/`A`, `StatBlock`/`Callout`/`ReliefCard`/`GuideCard`/`ConseilsCTA`, `WaveMotif`, `RelatedGuides`) + `GUIDE_META` (source unique des cartes + maillage interne). SEO par page via `useConseilsSeo` (title/description/canonical/og **+ injection d'un JSON-LD `Article`**, cf. SEO). Icônes : set `@/marketing/components/icons` étendu pour l'occasion (`ChevronRight, Clock, Users, Stethoscope, Calendar, Home, Utensils, Bell, Briefcase, Loop`). Images hero dans `public/conseils/` (`lecture-fenetre.jpg`, `the-fenetre.jpg` ; le hub/pilier réutilisent `/hero-etablissements.jpg`). CTA secondaire « Testez-nous en vrai » → `SIGNUP_URL` (`@/config/links`, résout `app.aicoute.fr/auth/register` en prod ; CTA primaire « Essayez » → `/#essai`). Chiffres sourcés (Baromètre Petits Frères des Pauvres 2025) — ne pas modifier sans validation édito. Lien « Conseils » dans le footer (bloc Entreprise).

Le **[Footer.tsx](apps/web/src/marketing/components/Footer.tsx)** est partagé home + sous-pages : ses liens-ancres sont en `/#section` (et NON `#section`) pour scroller sur la home ET naviguer-puis-scroller depuis une sous-page. Bloc Légal = Mentions légales / **CGU — CGV** (regroupés sur une ligne, CGV en nouvel onglet via un drapeau `newTab` sur l'entrée) / RGPD / IA Act ; bloc Entreprise = À propos / Conseils / Établissements / Charte éthique / Contact. (Les ancres nues `#…` restantes du Header/Hero/FAQ sont OK car ces composants ne vivent que sur la home.)

### SEO (vitrine)
- `index.html` : `<title>` + description, `canonical`, **Open Graph + Twitter Card**, **JSON-LD `@graph` STATIQUE sitewide** (`Organization` `#organization` avec `logo=/logo.png` + `sameAs` LinkedIn + `WebSite` + `FAQPage` 8 Q/R), `favicon.svg`, `theme-color`. Statiques → vues par Google ET bots sociaux sans JS. ⚠️ servies aussi sur `app.*` (même index.html) mais sans impact (back-office non indexé). L'`Organization` est **unique** ici (ne pas la dupliquer ailleurs).
- **JSON-LD `Article` par page** sur les 4 guides `/conseils/*` : **injecté CÔTÉ CLIENT** par `useConseilsSeo` (un `<script type="application/ld+json" data-conseils-ld>` ajouté au `<head>` au montage, retiré au démontage), construit par `buildArticleLd` (headline=H1, description=meta, `mainEntityOfPage`=canonique, image hero absolue, `author`/`publisher`=Aicoute+logo.png). Dates **réelles issues de git** (constantes `CONSEILS_PUBLISHED`/`CONSEILS_MODIFIED` dans `ConseilsLayout.tsx`). ⚠️ Étant injecté en JS, il **n'apparaît PAS dans le HTML brut (`curl`)** — Google + le Test des résultats enrichis exécutent le JS et le voient ; un crawler IA sans JS ne le verra pas (le rendre statique = étendre le prerender aux routes `/conseils/*`, non fait).
- **Logo en URL dédiée** : `public/logo.svg` (lockup picto + wordmark « aicoute » **vectorisé** depuis Fraunces 500 → fidèle sans dépendance police), `public/logo.png` (raster transparent 1108×256, = `Organization.logo`), `public/logo-mark.svg` (pictogramme seul). Régénérables via le script ponctuel `/tmp/logo-build` (woff2 Google Fonts → `wawoff2` → `opentype.js` outline → SVG/PNG). `favicon.svg` reste une version simplifiée du picto (tuile + ondes).
- `public/robots.txt` (+ `Disallow: /r/`, lien sitemap) + `public/sitemap.xml` (**statique, maintenu à la main** — 10 URL : home, /organisations, /etablissements, /municipalites, /assurances, /conseils + 4 guides ; host `www` sans slash final. Toute nouvelle page publique = ajouter l'entrée à la main).
- ⚠️ **DÉCISION EN SUSPENS — incohérence d'hôte `www` vs apex (à trancher, NE PAS faire à la légère).** Constat (vérifié au `curl` le 2026-06-19) : **`www.aicoute.fr` fait un 301 vers l'apex `aicoute.fr`** → l'hôte réellement servi (200) est **l'apex**. MAIS tout le code canonicalise vers **`www`** : les `<loc>` du sitemap, la ligne `Sitemap:` de robots.txt, les `canonical`/`og:url`/`twitter`/`@graph` (`@id`/`url`/`logo`) de `index.html`, et chaque `META.canonical` des pages marketing (`useMarketingSeo` + `useConseilsSeo`). Donc canonicals + sitemap pointent vers des URLs qui **redirigent** → signal brouillé pour Google (symptôme observé : GSC qui tarde à voir les nouvelles pages — en partie un simple lag de re-parse du sitemap, mais l'incohérence d'hôte n'aide pas). **Reco = canonicaliser sur l'APEX `aicoute.fr`** (s'aligne sur la redirection existante, zéro changement d'infra) : basculer en apex le sitemap, robots.txt, `index.html` (canonical/og/JSON-LD/og:image), `useMarketingSeo`/`useConseilsSeo` + tous les `META.canonical`, et les defaults Edge `PUBLIC_REPORT_URL`/`PUBLIC_SITE_URL` (laisser `www` dans `ALLOWED_ORIGINS` voice-bridge + Auth Supabase — il doit continuer à rediriger). Alternative (moins recommandée) : garder `www` comme vitrine → **inverser** la redirection Netlify (apex → www) dans `netlify.toml`, canonicals inchangés. **Non fait au 2026-06-19** (reporté par l'utilisateur, démo en cours) ; cf. mémoire `aicoute-www-vs-apex-pending`.
- **Image OG** `public/og-image.jpg` (1200×630, charte) régénérable via `node scripts/make-og-image.mjs` — script **PONCTUEL, hors build** : `npm i --no-save @resvg/resvg-js sharp` + polices TTF sous-settées Google Fonts (UA Android → TTF, param `&text=` = glyphes de la copie courante uniquement → re-télécharger si la copie change).

### Prerendering SSG de la home
`build` = `vite build && vite build --ssr src/entry-prerender.tsx --outDir dist-ssr && node scripts/prerender.mjs` :
- [entry-prerender.tsx](apps/web/src/entry-prerender.tsx) rend `<Home/>` en HTML via `react-dom/server` (sans navigateur).
- [scripts/prerender.mjs](apps/web/scripts/prerender.mjs) l'injecte dans `dist/index.html`. **Résilient** : en cas d'échec → garde l'index.html SPA (fonctionnel) et n'échoue PAS le build (dégradation SEO-only, jamais un deploy cassé) ; nettoie `dist-ssr` (gitignoré).
- [main.tsx](apps/web/src/main.tsx) : `hydrateRoot` **uniquement** sur la home vitrine pré-rendue (`pathname === '/' && !hostname.startsWith('app.')`) ; sinon `root.replaceChildren()` + `createRoot` (l'index.html servi sur TOUTE route contient le markup home → sans ça, mismatch d'hydratation sur les sous-pages).
- Marche car les composants react-router n'émettent aucun DOM → markup de `<App/>` sur "/" === `<Home/>`. ⚠️ Si la home utilise un jour une valeur non-déterministe au rendu (`Date`/`Math.random`) ou un accès `window`/`document` hors `useEffect`, l'hydratation casse.

## Variables d'environnement

### apps/web (.env) — app unique (vitrine + back-office)
```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=      # clé publique anon
VITE_APP_URL=https://app.aicoute.fr
VITE_DASHBOARD_URL=          # https://app.aicoute.fr (prod) / vide en local (liens relatifs)
VITE_VOICE_BRIDGE_URL=       # https://voice.aicoute.fr (prod) / vide en local (cache le bouton "Me faire appeler")
```

### Supabase Edge Functions Secrets
```
OPENAI_API_KEY=
RESEND_API_KEY=
FROM_EMAIL=
DEMO_TRACK_KEY=              # secret pour accéder à /track_calls (générer une chaîne longue aléatoire)
VOICE_BRIDGE_URL=            # URL du service Render (ex: https://voice.aicoute.fr) — utilisé par initiate-call pour POST /scheduled-call
MODECT_INTERNAL_TOKEN=       # secret partagé Supabase ↔ voice-bridge pour les appels planifiés (générer via `openssl rand -hex 32`)
PUBLIC_REPORT_URL=           # optionnel — base URL de la page publique de compte-rendu /r/:token (défaut https://www.aicoute.fr)
MODEL_WATCH_MODEL=           # optionnel — modèle OpenAI (Responses API + web_search) pour la veille /admin/sante (défaut gpt-4.1)

# --- Stripe (achat de minutes) ---
STRIPE_SECRET_KEY=           # clé secrète Stripe (idéalement clé restreinte rk_live_ « Checkout Sessions: write » — compte partagé). EN ATTENTE (2026-06-10)
STRIPE_WEBHOOK_SECRET=       # signing secret de l'endpoint webhook (whsec_…) → vérif signature dans stripe-webhook
STRIPE_PRICE_RENDEZVOUS=     # Price ID Stripe du pack 50 min
STRIPE_PRICE_LIEN=           # Price ID Stripe du pack 100 min
STRIPE_PRICE_PRESENCE=       # Price ID Stripe du pack 250 min
STRIPE_PRICE_CONTROLE=       # Price ID Stripe RÉCURRENT (18€/mois) de l'abonnement « Le contrôle » (mode subscription ; parcours paiement-d'abord)
PUBLIC_SITE_URL=             # optionnel — base vitrine pour success/cancel (défaut https://www.aicoute.fr)
APP_URL=                     # optionnel — base back-office pour retour achat connecté (défaut https://app.aicoute.fr)
```

### services/voice-bridge (.env) — service Render
```
OPENAI_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_NUMBER=+33939035269
ALLOWED_ORIGINS=https://www.aicoute.fr,https://aicoute.fr,https://app.aicoute.fr,https://app.modect.com  # CRITIQUE : doit inclure (1) les origines qui ouvriront /ws/gemini-web ET (2) l'origine du BACK-OFFICE pour le test d'appel admin /admin/sante (POST /call). Le back-office est désormais servi en prod sur **app.aicoute.fr** (bascule effectuée — sous-domaine en place 2026-06) → c'est l'origine réelle. `app.modect.com` (ancien domaine) reste listé par sécurité le temps de la transition ; retirable une fois confirmé qu'il n'est plus utilisé. Sans l'origine du back-office → preflight sans Access-Control-Allow-Origin → « Failed to fetch » côté navigateur
MAX_CALL_SECONDS=120                # démo vitrine
MAX_SCHEDULED_CALL_SECONDS=900      # FILET global appels AICOUTE (15 min max) si aucune durée de planning transmise
SCHEDULED_DURATION_GRACE_SECONDS=60 # marge ajoutée à la durée du planning pour la coupure DURE des appels planifiés (laisse l'IA conclure sans couper en plein « au revoir »). Coupure = min(max_duration_minutes*60 + marge, MAX_SCHEDULED_CALL_SECONDS)
SCHEDULED_RING_TIMEOUT=30           # sonnerie max Twilio avant raccrochage auto
MAX_CONCURRENT_CALLS=0       # contrôle d'admission appels vocaux simultanés. 0 = illimité/désarmé (défaut). La mesure du pic tourne quoi qu'il arrive (system_events source voice-bridge/admission). Armer à une vraie valeur (ex. 20) quand le plafond Render est connu.
RECORD_ALL_CALLS=            # défaut ON (toute valeur ≠ "0"). Enregistre TOUS les appels (démo + planifiés + entrants) en WAV dual-channel → bucket fluidity-recordings + rattaché à la ligne d'appel (recording_path) → bouton « .wav » dans /admin/appels (passés/émis/démos). ⚠️ RGPD : couvre aussi les vrais appels bénéficiaires (phase de TEST). Mettre "0" pour désarmer avant la prod réelle.

# --- Analyse fluidité « vérité terrain » sur le WAV (engines/wav-analysis.js) ---
# Seuils VAD de l'analyse OFFLINE du WAV dual-channel. À CALIBRER contre Audacity.
WAV_FRAME_MS=                # défaut 20 — fenêtre RMS
WAV_HANG_MS=                 # défaut 250 — silence continu → fin de parole
WAV_ONSET_MS=                # défaut 90 — parole continue → début (rejette les clics)
WAV_VAD_FACTOR=              # défaut 3.5 — seuil = bruit de fond × facteur
WAV_VAD_MIN_RMS=             # défaut 250 — plancher absolu d'énergie (PCM16)
WAV_AI_CHANNEL=              # vide = canal 1 (jambe SORTANTE Twilio = IA, déterministe) ; "0"|"1" pour forcer. NB : l'auto-détection « 1er à parler » a été abandonnée (l'interlocuteur dit « allô » avant le bonjour → inversion)
WAV_GREETING_MIN_MS=         # défaut 400 — durée mini d'un VRAI segment de parole ; sert à dater start_ms (ignore un clic résiduel à t=0)
GREETING_FALLBACK_MS=0       # défaut 0 = bonjour proactif immédiat (validé) ; > 0 = mode hybride « attendre le allô » (abandonné)
GREETING_PROTECT_MAX_MS=8000 # porte micro : durée MAX de protection du bonjour (rouverte au 1er turnComplete sinon)
SUPABASE_URL=                # écriture demo_calls (tracking /track_calls) + calls (appels planifiés)
SUPABASE_SERVICE_ROLE_KEY=   # idem ; si absents → persistance désactivée silencieusement
MODECT_INTERNAL_TOKEN=       # MÊME valeur que côté Supabase ; si absent → /scheduled-call refusé en 503

# --- Gemini Live (optionnel — sans ces vars, l'engine 'gemini' est refusé en 503) ---
GOOGLE_API_KEY=              # clé Google AI Studio (https://aistudio.google.com/apikey)
GEMINI_MODEL=                # défaut : models/gemini-3.1-flash-live-preview ; override si Google publie un nouveau preview label
GEMINI_VOICE=                # FALLBACK voix Gemini si le bénéficiaire n'en a pas (gemini_voice) ; défaut Aoede. Les appels planifiés utilisent désormais la voix par bénéficiaire (ctx.gemini_voice) ; cet env ne sert qu'aux démos vitrine + filet de secours.

# --- VAD / fluidité Gemini (optionnel — cf. « Fluidité de la conversation » plus haut) ---
GEMINI_VAD_DISABLED=         # true → kill-switch, retour au comportement Gemini par défaut
GEMINI_VAD_START_SENSITIVITY=   # défaut : START_SENSITIVITY_LOW
GEMINI_VAD_PREFIX_PADDING_MS=   # défaut : 300 (ms de parole soutenue requis avant interruption ; ↑ = rejette + le bruit)
GEMINI_VAD_END_SENSITIVITY=     # anti-« blanc » (optionnel, non envoyé par défaut ; END_SENSITIVITY_HIGH = fin détectée + tôt)
GEMINI_VAD_SILENCE_DURATION_MS= # anti-« blanc » (optionnel, non envoyé par défaut ; ex. 600-800)

# --- Endpointing Gemini (MESURE du « blanc » téléphone — observation, ne change rien à ce que Gemini entend) ---
GEMINI_ENDPOINT_DISABLED=    # true → kill-switch : retour au proxy transcript (blank approx=true, sous-estimé)
GEMINI_ENDPOINT_HANG_MS=     # défaut 350 : silence continu avant de déclarer la fin de parole
GEMINI_ENDPOINT_ONSET_MS=    # défaut 80 : parole continue avant de confirmer un début (rejette les clics)
GEMINI_ENDPOINT_MIN_RMS=     # défaut 500 : plancher absolu d'énergie (PCM16) pour considérer une frame comme parole

# --- Latence de génération Gemini (anti-« blanc » de ~2s, prouvé = génération, pas VAD) ---
GEMINI_THINKING_BUDGET=      # non défini (défaut) = non envoyé. "0" = désactive le « thinking » (raisonnement
                             #   interne avant de répondre) qui ajoute ~2s de latence avant le 1er son. "<n>" = budget
                             #   de tokens de réflexion limité. OPT-IN : si le modèle Live n'accepte pas thinkingConfig,
                             #   le setup échoue (1007) → à valider en test, OFF par défaut pour ne pas casser la prod.

# --- VAD / fluidité OpenAI (optionnel — cf. « Fluidité de la conversation » plus haut) ---
OPENAI_VAD_DISABLED=         # true → kill-switch, retour aux défauts OpenAI
OPENAI_VAD_TYPE=             # défaut : semantic_vad (anti-« blanc ») | server_vad
OPENAI_VAD_EAGERNESS=        # semantic_vad : low|medium|high|auto (défaut high ; ↓ = attend plus)
OPENAI_NOISE_REDUCTION=      # défaut : far_field (haut-parleur) | near_field (combiné) | off
OPENAI_VAD_THRESHOLD=           # server_vad only (défaut 0.5 ; ↑ = exige voix plus franche)
OPENAI_VAD_PREFIX_PADDING_MS=   # server_vad only (défaut 300)
OPENAI_VAD_SILENCE_DURATION_MS= # server_vad only (défaut 500 ; ↓ = répond + vite)
```

## Déploiement
- **Netlify** : **un seul site** (Base directory `apps/web`, `apps/web/netlify.toml`). Faire pointer **les deux domaines** `www.aicoute.fr` + `app.aicoute.fr` vers ce site. L'app route selon le sous-domaine (`src/App.tsx` : `app.*` → back-office, sinon vitrine). `Permissions-Policy: microphone=(self)` pour WebRTC. Penser à whitelister `app.aicoute.fr` dans Supabase → Auth → URL Configuration.
- **Analytics (Umami)** : snippet chargé **conditionnellement** depuis `apps/web/index.html` via un garde inline — uniquement sur la **vitrine** (exclut `app.*` + `localhost`/`127.0.0.1`), pour ne pas tracker le back-office ni le dev local. `data-website-id` Umami Cloud en dur dans `index.html`.
- **Supabase** : `supabase link --project-ref XXX` puis `supabase functions deploy`
- **Render** : Web Service Node pour `services/voice-bridge` (plan **Starter** minimum — le Free dort après 15 min et casse la démo). Région Frankfurt. Custom domain `voice.aicoute.fr` (CNAME). Variables à renseigner dans l'UI Render. Blueprint disponible dans `services/voice-bridge/render.yaml`.
- **pg_cron** : cron toutes les minutes → appelle `schedule-calls` via `pg_net`. Les secrets nécessaires (`supabase_url`, `service_role_key`) sont stockés dans **Supabase Vault** et lus via `vault.decrypted_secrets` (cf. ci-dessous — Supabase managé interdit `ALTER DATABASE SET app.settings.*`).

## Bugs connus et fixes appliqués
- `handle_new_user()` trigger : doit avoir `SET search_path = public` sinon "relation profiles does not exist"
- `formatDate()` dans utils.ts : `dateStyle` incompatible avec options individuelles (`weekday`, `hour`...) — utiliser `options ?? { dateStyle: 'long' }`
- `@modect/shared` dans le web : résolu via alias Vite + paths TypeScript dans tsconfig
- Build Netlify : utiliser `vite build` sans `tsc` (types Supabase incomplets génèrent des `never`)
- Realtime GA : token dans `response.value` (PAS `response.client_secret.value` = Beta) ; endpoint `/v1/realtime/calls` (PAS `/realtime`) ; events `response.output_audio_transcript.*` (fallback Beta `response.audio_transcript.*`)
- `calls.livekit_room_name` / `livekit_room_sid` : colonnes héritées désormais inutilisées (schéma conservé, non écrites)
- **ws v8 multi-WSS** : créer deux `WebSocketServer({ server, path })` sur le même HTTP server NE marche PAS — le 1er WSS appelle `abortHandshake(400)` sur tout path qui ne lui correspond pas, empêchant le 2e WSS de répondre (proxy renvoie 404). Voice-bridge utilise donc `noServer: true` + dispatch manuel `server.on('upgrade')` selon `req.url`.
- Gemini Live model ID : `models/gemini-2.5-flash-native-audio` n'existe pas via v1beta ; le bon ID est `models/gemini-3.1-flash-live-preview` (2026-05). Google bouge les preview labels — surcharger via env `GEMINI_MODEL` plutôt que modifier le code.
- **Supabase managé sans `ALTER DATABASE`** : `ALTER DATABASE postgres SET app.settings.*` est refusé (`permission denied to set parameter`, superuser only). Du coup `current_setting('app.settings.supabase_url')` retourne NULL et toute fonction trigger qui s'en servait fait silencieusement `pg_net.http_post('NULL/...')`. **Solution** : stocker les secrets dans `vault.secrets` (`SELECT vault.create_secret('https://xxx.supabase.co', 'supabase_url')`) et les lire via `vault.decrypted_secrets` depuis une fonction `SECURITY DEFINER` (ex: `trigger_regenerate_future_calls`). Le cron `modect-schedule-calls` suit le même pattern.
- **Postgres `ON CONFLICT` + UNIQUE partial index** : un index `CREATE UNIQUE INDEX ... WHERE ...` ne peut être utilisé par `INSERT ... ON CONFLICT (col)` que si on **répète la clause WHERE**, ce que PostgREST/`supabase.upsert()` ne sait pas faire → erreur « there is no unique or exclusion constraint matching the ON CONFLICT specification ». **Solution** : utiliser un UNIQUE constraint non-partial (les NULL sont distincts par défaut, donc même sémantique fonctionnelle pour les colonnes nullable).
- **`EdgeRuntime.waitUntil` instable** : le pattern fire-and-forget pour chaîner deux Edge Functions (`fetch().catch()` + `EdgeRuntime?.waitUntil`) n'est pas garanti côté runtime Supabase — la fetch est souvent garbage-collected avant d'arriver à destination. **Symptôme observé** : `save-transcript` n'invoquait jamais `generate-summary` en pratique → ni résumé, ni alertes, ni email. **Solution** : `await` la fetch même si ça allonge la latence (la WS Twilio appelante est déjà fermée à ce stade donc impact UX nul).
- **`verify_jwt=true` rejette la clé service-role en 401** : une Edge Fn appelée en interne par une autre (via `fetch` + `Bearer ${SERVICE_ROLE_KEY}`) doit être en `verify_jwt = false`. Avec `verify_jwt=true`, la passerelle Supabase accepte la clé **anon** mais **rejette la service-role en 401**. Symptôme vécu : `generate-summary` (seule en `verify_jwt=true`) renvoyait 401 à `save-transcript` / `schedule-calls` passe D / `resend-report` → ni compte-rendu ni email, en silence. **Toutes les Edge Fn internes du projet sont en `verify_jwt=false`** (auth gérée en interne).
- **Table créée par migration SQL brute = pas de GRANT** : `CREATE TABLE` + `ENABLE RLS` + policies ne suffit PAS — sans `GRANT SELECT/UPDATE … TO anon, authenticated`, PostgREST renvoie « permission denied for table … » côté client AVANT d'évaluer la RLS. Vécu sur `system_events` (`…0008`) puis `prompt_templates` (`…0005`). ⚠️ **Le `service_role` n'hérite PAS non plus des privilèges par défaut sur ce projet** : une Edge Fn qui écrit en service-role dans une table fraîchement créée échoue aussi en « permission denied » → il faut **`GRANT … TO service_role`** explicitement (vécu sur `app_settings` `…0002`, puis sur `minute_adjustments`/`minute_codes`/`minute_purchases` `…20260610000003`, **puis sur `subscriptions` `…20260702000002`** : la table n'était grantée qu'à `authenticated` — l'essai gratuit s'insère côté client — donc le 1er write service-role, `claim-control-subscription`, échouait en « permission denied » ; la ligne `pending_control_subscriptions` passait `claimed` sans qu'aucune ligne `subscriptions` ne soit créée). Pour toute nouvelle table **ET toute table où une Edge écrit pour la 1ʳᵉ fois en service-role** : GRANT pour `authenticated` (lecture client) ET `service_role` (écriture Edge).
- **Resend domain verification** : le DKIM verified ne suffit pas pour autoriser l'envoi — il faut aussi SPF + (idéalement) MX vérifiés. Et l'API key par défaut (créée à l'inscription) est **scopée au premier domaine** ; pour envoyer depuis un nouveau domaine il faut créer une nouvelle key avec **Domain = All domains** (mode Full access).
- **Edge Functions tournent en UTC** : `toLocaleString`/`toLocaleDateString` SANS option `timeZone` formate en UTC → toute date affichée dans un email (heure d'appel, etc.) est décalée (ex. −2h l'été en France). **Toujours passer `timeZone`** : fuseau de l'entité concernée (`session_schedules.timezone` pour un appel planifié, `profiles.timezone` pour l'aidant) avec repli `'Europe/Paris'`. Vécu sur l'email no-answer + les comptes-rendus `generate-summary`/`resend-report` (corrigé 2026-06-29).

## Identité visuelle — charte « cocon familial » (apps/web : vitrine + back-office)
- **Couleurs** : terracotta `#C75D3A` (primaire) + ocre `#D9943E` (accent) ; texte brun `#3D2817`/`#6B4423` ; fonds crème `#FBF5EE` / crème sable `#F5EBDC` ; succès sauge `#7BA05B`, erreur brique `#B23A48`
- **Polices** : Fraunces (titres) + Inter (corps)
- **Baseline** : "Une présence pour ceux que vous aimez" (vitrine) / "La présence qui réchauffe" (app)
- **Back-office** : tokens Tailwind `primary`=terracotta, `accent`=ocre (échelles 50–900), palette `slate` réchauffée (neutres taupe/brun) — cf `apps/web/tailwind.config.js`. La vitrine utilise en plus `font-serif`/`font-sans`, `max-w-container`, `tracking-widest`.
- **Accessibilité mobile** : police ≥ 18px, boutons ≥ 72px, pas de gestes complexes

## Commandes utiles
```bash
# Dev app web unique (vitrine + back-office) — port 5173
npm run dev          # ou : cd apps/web && npm run dev

# Déployer les fonctions Supabase
supabase functions deploy schedule-calls
supabase functions deploy initiate-call
supabase functions deploy realtime-token
supabase functions deploy public-realtime-token
supabase functions deploy get-call-context     # appelée par voice-bridge (auth: MODECT_INTERNAL_TOKEN)
supabase functions deploy regenerate-future-calls  # pré-création des calls 15 jours à l'avance
supabase functions deploy log-demo
supabase functions deploy list-demos
supabase functions deploy save-transcript
supabase functions deploy generate-summary
supabase functions deploy get-report           # page publique de partage du compte-rendu (/r/:token, sans login)
supabase functions deploy list-openai-models
supabase functions deploy admin-update-caregiver  # édition aidant (propage email → auth.users)
supabase functions deploy admin-delete-caregiver  # suppression aidant (refuse si bénéficiaires)
supabase functions deploy model-watch             # veille modèles voix (OpenAI Responses + web_search, admin only)
supabase functions deploy create-checkout-session # Stripe Checkout (achat de minutes + abonnement « Le contrôle »)
supabase functions deploy stripe-webhook          # Stripe webhook (crédit minutes + rétention abonnement « Le contrôle »)
supabase functions deploy get-purchase-code       # page succès invité (/achat/merci) — poll du code
supabase functions deploy get-control-checkout    # pré-remplissage inscription depuis un paiement « Le contrôle » (session_id → email)
supabase functions deploy claim-control-subscription # rattache l'abonnement « Le contrôle » au compte à l'inscription
supabase functions deploy create-billing-portal-session # portail client Stripe (factures/gestion abonnement « Le contrôle »)
supabase functions deploy redeem-code             # crédit d'un code d'achat (aidant connecté)
supabase functions deploy admin-credit-minutes    # crédit manuel admin (minute_adjustments)
supabase functions deploy admin-prompts           # CRUD bibliothèque de prompts (admin only)
supabase functions deploy get-call-context        # ⚠️ à redéployer : lit désormais les défauts de `prompts`
supabase functions deploy realtime-token          # ⚠️ idem (partage callContext.ts)

# Push migrations
supabase db push

# Voice-bridge (démo téléphone vitrine) — dev local
cd services/voice-bridge && npm run dev
```
